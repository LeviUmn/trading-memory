---
name: project-regelwerk-ueberarbeitung-2026-08-26
description: "Laufende Regelwerk-Überarbeitung nach Opus-Testtag-Analyse 25.08.2026 — drei Pakete an Fable vergeben, Opus-Endverifikation noch ausstehend. Muss auch über Usage-Limit-Resets hinweg fortgesetzt werden (Levi-Vorgabe)."
metadata:
  node_type: memory
  type: project
  status: in_progress
  originSessionId: session-2026-08-26
  modified: 2026-08-27T09:25:35.009Z
---

# Kontext

Nach dem fiktiven Testtag 25.08.2026 fand Opus einen Kernbefund (siehe [[project_testtag_analyse_2026-08-25]]): #T1/#T3 arithmetisch unmöglich, weil `gate_check.cjs` nie tatsächlich aufgerufen wurde und der SL-Floor (1,5x ATR) unterschritten war. Daraus entstand eine Kette von Nachfragen (Levi → Opus → Fable), die am 26.08.2026 in drei Umsetzungspaketen an Fable mündete. Levi hat explizit angewiesen: **"Wenn gleich das Limit erreicht ist von Claude, dann bitte automatisch weiter machen mit der Umsetzung, sobald der Reset da ist. Nichts vergessen und danach nochmal prüfen von Opus, ob Fable alles korrekt umgesetzt hat!"**

## Die drei Pakete an Fable (Agent-ID im Ursprungssession-Kontext: ad5c117c126b8d82b, Name könnte in neuer Session nicht mehr auflösbar sein — dann anhand der Datei-Änderungen selbst nachvollziehen, was bereits umgesetzt ist)

**Paket 1** (SL/RR-Kernbefund): `scripts/gate_check.cjs` neues Hard-Gate `zoneRrConsistencyGate` (1,333-Konsistenztest), `feedback_live_trading.md` Punkt 7b1 verschärft (5-Schritte-Ablauf + Pflicht zum tatsächlichen Skript-Aufruf mit wörtlichem Output), Rohwert-Pflichtzeilen (SL/ATR-Ratio, Kerze-geschlossen) statt nur Urteile, ATR-Zeitebene auf 5min/NAS100 festgeschrieben, neuer Punkt 15 "Solo-Mandat". **Status: von Fable als abgeschlossen gemeldet UND von Opus verifiziert (korrekt, siehe unten) — aber Opus fand danach eine neue, größere Lücke (Paket 2).**

**Paket 2** (Tick-Prompt-Vollständigkeit + Live-Chart-Fixes, ausgelöst durch Opus' Befund: nur 3 von ~12 Pflichtelementen stehen im tatsächlichen CronCreate-Pflichtbaustein Punkt 2b):
- Punkt 2b komplett neu fassen: Stall-Check, Positions-Kasten, Regime-Gate-Zeile, Volumen-Check/RVOL, Chop-Check, Spike-Ausnahme, Basis-Reclaim-Klasse, cooldown_check.cjs vor Entry, 5-Schritte-gate_check.cjs-Ablauf, Solo-Mandat — ALLE müssen jetzt im tatsächlichen Tick-Prompt stehen, nicht nur im Regelwerk-Fließtext.
- AVWAP-Remote-Fallback auf QQQ live untersuchen/reparieren (Anker-Markierung stand bei 0,00, Werte identisch mit eingebauter VWAP) — wenn nicht reparierbar: eingebaute VWAP entfernen + Pflicht-Neuverankerung dokumentieren.
- Neue Session-Start-Checkliste für Indikatoren (RVOL nicht leer/0, AVWAP-Anker ≠ 0,00).
- Indikator-Inventar in `feedback_chartanalyse.md` Punkt 6 korrigieren (veraltet: "3+3", tatsächlich 6 NAS100 / 5 QQQ).
- ADX-Ausgabezeile ergänzen (existiert in DB/Skript, aber keine Pflichtzeile).
**Status: von Fable umgesetzt (26.08.2026), wartet auf Opus-Endverifikation.** Kurzfassung je Punkt: (1) Punkt 2b in `feedback_live_trading.md` um einen vollständigen "Vollständiger Soll/Ist-Abgleich 26.08.2026"-Block mit allen 12 Elementen erweitert (4 davon — Stall-Check/Positions-Kasten/Volumen-Check/Punkt-11-Kriterien — jetzt explizit bei JEDEM Check-in inkl. Quick-Tick, nicht nur Voll-Check). (2) AVWAP live untersucht via MCP-Tools (`pane_focus`/`chart_get_state`/`data_get_study_values`/`data_get_indicator`): `in_0`=0 bestätigt (Fallback), eingebaute VWAP-Studie per `chart_manage_indicator` von QQQ ENTFERNT (nicht nur dokumentiert), Layout gespeichert, per Screenshot verifiziert — Anker selbst bewusst NICHT auf einen erfundenen Wert gesetzt (siehe Begründung in `feedback_chart_layout.md`); zusätzlich Korrektur an der ursprünglich vorgeschlagenen "Anker-Markierung ungleich 0,00"-Prüfung gefunden und dokumentiert (technisch ungeeignet, korrekt ist der `in_0`-Rohwert). (3) Session-Start-Checkliste NICHT als zweite eigenständige Liste in `feedback_live_trading.md` angelegt, sondern in `feedback_session_update.md` Schritt 6 konsolidiert (Absprache mit dem Paket-3-Prüfauftrag, siehe dort) — `feedback_live_trading.md` Punkt 2b verweist nur dorthin. (4) `feedback_chartanalyse.md` Punkt 6 auf 6 NAS100 / 4 QQQ (nach VWAP-Entfernung, nicht mehr 5) korrigiert. (5) ADX-Zeile in `feedback_vollcheck_format.md` + Punkt 2b ergänzt.

**Paket 3** (`feedback_session_update.md` — "start update dich"-Mechanismus, ausgelöst durch Levis Frage + Opus-Analyse):
- Pivot-Neuberechnung (7a1a) fest als ERSTER Teilschritt in Schritt 6 verankern (war seit 24.08. nur als nie umgesetztes To-do in der Datei selbst vermerkt).
- Echte Bias-Synthese-Regel schreiben (wie werden 5-6 Quellen zu einem Urteil, Vorrangregel bei Widerspruch, analog zu `feedback_chartanalyse.md` Punkt 8 "1H-Bias überschreibt 5min"), UND explizit klären ob der Morgen-Bias für Entries bindend ist oder nur Kontext.
- Kleinere Lücken: Phase-/Sizing-Kontext in Schritt 1, VIX→Hebel-Staffelung-Einzeiler in 5a, Hinweis auf 15:30-16:00-Halbierungsfenster, Klarstellung der Schritt-2/4-Abhängigkeit.
- Überschneidungen mit Paket 2 (Indikator-Check, cooldown_check.cjs) sollen konsolidiert, nicht dupliziert werden — das war Fable explizit als Prüfauftrag mitgegeben.
**Status: von Fable umgesetzt (26.08.2026), wartet auf Opus-Endverifikation.** Kurzfassung: To-do-Absatz aus der Datei entfernt, Pivot-Berechnung als fester erster Teilschritt in Schritt 6 direkt nach `chart_get_state` verankert. Neuer Abschnitt "Bias-Synthese" mit expliziter Rangfolge (Chart-Struktur > Makro-Sperrfristen/Schock-Tweets > Intermarket-Widerspruch-Regel > FRED/struktureller Tweet-Kontext) UND expliziter Bindungsklärung: Morgen-Bias ist Kontext/Orientierung, KEIN Gate für den Live-Loop (Ausnahme: bereits bestehende harte Zeit-/Blackout-Regeln bleiben unverändert bindend). Phase-/Sizing-Kontext + Cooldown-Status-Hinweis in Schritt 1 ergänzt, VIX→Hebel-Tabelle-Einzeiler in Schritt 5a, Halbierungsfenster-Reminder ergänzt, Schritt-2/4-Abhängigkeit explizit klargestellt (Parallelität = Startzeitpunkt, Cross-Check-Auswertung bleibt abhängig). Indikator-Vollständigkeits-Check (RVOL + AVWAP-`in_0`) AUSSCHLIESSLICH hier in Schritt 6 verankert, nicht dupliziert. Kurzer Querverweis in `feedback_chartanalyse.md` Punkt 8 ergänzt.

## Offener Abschlussschritt (NICHT VERGESSEN)

Sobald Fable alle drei Pakete (oder mindestens Paket 2+3, falls Paket 1 schon separat bestätigt ist) abgeschlossen hat: **Opus muss ein zweites Mal die tatsächlich geänderten Dateien selbst lesen und verifizieren** (nicht nur Fables Zusammenfassung glauben — exakt wie beim ersten Verifikationsdurchlauf zu Paket 1). Prüfpunkte mindestens:
1. Ist der neue Punkt-2b-Tick-Prompt-Text tatsächlich vollständig (alle ~12 Elemente) und widerspruchsfrei in den bestehenden Text integriert?
2. Wurde die AVWAP-Frage tatsächlich live am Chart gelöst oder nur dokumentiert (Fable sollte das explizit unterscheiden)?
3. Ist die neue Bias-Synthese-Regel in `feedback_session_update.md` konkret genug (keine vage Formulierung, echte Vorrangregel)?
4. Ist die Pivot-Verankerung tatsächlich als Pflichtschritt (nicht nur Erwähnung) eingebaut?
5. Wurden die Paket-2/Paket-3-Überschneidungen sauber konsolidiert statt dupliziert?
6. Gibt es neue Widersprüche zwischen den frisch geänderten Dateien?

**Diese Aufgabe gilt erst als erledigt, wenn Opus dieses zweite Verifikations-Urteil abgegeben UND Levi es gesehen hat.** Falls diese Session durch ein Usage-Limit unterbrochen wird: nach dem Reset zuerst prüfen, ob Fables Task-Notifications bereits vorliegen (Agent-Name/ID ggf. über die Konversationshistorie/den Transkript-Kontext dieser Session rekonstruieren, falls verfügbar), dann diesen Abschlussschritt (Opus-Endverifikation) nachholen, dann Levi das Ergebnis unverändert mitteilen.

**Stand 26.08.2026, nach Fables Rückmeldung:** Paket 2 UND Paket 3 sind laut Fable jetzt ebenfalls umgesetzt (Kurzfassungen oben bei den jeweiligen Paketen ergänzt) — alle drei Pakete warten damit auf denselben Abschlussschritt: die Opus-Endverifikation gegen die tatsächlich geänderten Dateien (`feedback_live_trading.md`, `feedback_chartanalyse.md`, `feedback_chart_layout.md`, `feedback_vollcheck_format.md`, `feedback_session_update.md`, `scripts/gate_check.cjs`, live-Chart-Zustand auf QQQ). Diese Verifikation NICHT vergessen, auch wenn Fables eigene Zusammenfassung bereits vollständig klingt — genau das war die explizite Levi-Vorgabe oben.

**Paket 4 (klein, Opus-Endverifikations-Nachbesserung, 26.08.2026):** Opus hat die Endverifikation von Paket 2+3 durchgeführt — inhaltlich korrekt, aber 3 kleine Inkonsistenzen gefunden, alle von Fable direkt behoben: (1) Widersprüchlicher Satz in `feedback_session_update.md` Schritt 6 ("KEINE Pflichtzeile im 1-Min-Loop" für ADX) korrigiert — klargestellt, dass sich das nur auf den 1H/Daily-Kontextwert bezieht, der 5min-Wert hat seit Paket 2 eine Pflichtzeile. (2) Tote Studienname-Referenzen "VWAP" (ohne "(Remote)") für QQQ in `feedback_live_trading.md` (Punkt 2b MTF-Block), `feedback_session_update.md` (Schritt 6) und `feedback_loop_ablauf_uebersicht.md` korrigiert. (3) Neue offene Frage dokumentiert (noch NICHT technisch gelöst, bewusst offen bis zum ersten echten AVWAP-Anker-Einsatz): Sigma-Band-Bezugsgröße verschiebt sich beim Setzen eines echten Ankers von Session-VWAP auf Anker-VWAP — Hinweis dazu in `feedback_chartanalyse.md` 8a4, `feedback_live_trading.md` Punkt 11 (Sechster Kontextfaktor) und `feedback_chart_layout.md` (AVWAP-Beschreibung) ergänzt, mit Querverweisen zwischen allen dreien. **Status: erledigt.**

**Paket 5 (klein, Opus-Empfehlung nach Pine-Code-Analyse, 26.08.2026, Levi-Auftrag "Ja bitte so an Fable geben"):** Der Entry-AVWAP-Anker (Punkt 11, Sechster Kontextfaktor) war seit 24.08.2026 nur als Konzept beschrieben, ohne konkreten Ausführungsschritt für Sonnet im Live-Loop — dieselbe Fließtext-ohne-Ausführung-Lücke wie bei den übrigen dieswöchigen Funden. Bewusst GESTUFT freigegeben: NUR der Entry-Fall (mechanisch, kein Ermessen), der Q1-Beleg-Fall (7b1a) bleibt vorerst deaktiviert. Umgesetzt in `feedback_live_trading.md`:
- Punkt 11: neuer Absatz "Ausführungsschritt: Entry-AVWAP-Anker jetzt tatsächlich automatisiert setzen" — Auslösemoment (direkt nach Entry, vor erstem Positions-Kasten), Ankerwahl (Kerzenbeginn der Entry-Kerze), exakter Tool-Call (`chart_get_state`/`pane_list` für aktuelle Entity-ID → `indicator_set_inputs` mit `in_0` in SEKUNDEN, expliziter ms/s-Warnhinweis), Pflicht-Rücklese-Verifikation via `data_get_indicator` (Grund: die Studie fällt bei ungültigem Wert kommentarlos in den Fallback zurück, ohne Fehlermeldung), und der explizit dokumentierte Nutzungskonflikt-Vorbehalt zum Q1-Fall (bleibt deaktiviert, bis geklärt ist, wer bei Kollision Vorrang hat).
- Punkt 8 (Positions-Kasten): Querverweis, dass der Anker-Schritt vor der ersten Kasten-Ausgabe erledigt sein muss.
- Punkt 2b (CronCreate-Pflichtbaustein): neuer Punkt (12) "Direkt NACH jeder tatsächlichen Order-Ausführung" mit demselben Ablauf, damit die Regel auch im tatsächlichen Tick-Prompt steht, nicht nur im Fließtext (Solo-Mandat entsprechend zu (13) umnummeriert).
**Status: erledigt.** Nächster Schritt bleibt unverändert die finale Opus-Bestätigung (inzwischen aller 6 Pakete inkl. 6a/6b/6c, siehe unten), dann gilt die gesamte Regelwerk-Überarbeitung als abgeschlossen.

**Paket 6 (27.08.2026, ausgelöst durch Levis Frage zur Short-Erkennung + Opus-Drittanalyse, siehe [[project_testtag_analyse_2026-08-24]] Abschnitt 11):** 7 priorisierte Punkte, alle Schattenmessung/Anzeige — kein Gate/Sizing-Eingriff. **Status: von Fable umgesetzt (27.08.2026), wartet auf Opus-Endverifikation.** Kurzfassung je Punkt:
1. (höchste Priorität, DB) `dual_gate_*`-Felder plus `kerzen_seit_gegenseite_nas100/qqq` und `pivot_distanz_punkte` als neue Spalten in `trades`-Tabelle (`trade_db.cjs` SCHEMA_SQL + NEW_COLUMNS + GATE_RESULT_COLUMNS + UPSERT_SQL + upsertTrade). `gate_check.cjs` empfängt die neuen Felder als optionale CLI-Parameter (--dual-gate-qqq-abstand-pct etc.), zeigt sie als GEMESSEN-KEIN-GATE-Zeilen an und schreibt sie bei --trade-id in die DB zurück. Live-Test erfolgreich (Node).
2. (B3-Zweitanker) Klarstellung in `feedback_live_trading.md` Punkt 7b1a: `--impuls-reifegrad-atr-emaanker` gilt für LONG UND SHORT (absoluter EMA50-Abstand / ATR), beide Richtungen gleichbehandelt.
3. (Stale-Klasse) Neuer Abschnitt 7b1c in `feedback_live_trading.md`: Vorbedingung (Dual-Gate erfüllt AND letzter Gegenseiten-Schluss NAS100 ≥6 Kerzen zurück), 5 Bedingungen (Gate-Alter NAS100 5min/QQQ 15min, Gate-Reserve, Momentum-Verfall ADX, Struktur gegen Richtung Zwei-Zweig, Indikator-Richtung MACD-H/Trend-Effizienz), Ausgabe als eigene Zeile im Entry-Block NUR bei erfüllter Vorbedingung. Konsequenz bei STALE: KEINE — reiner Anzeige-Modus, keine Rückfrage (Erstumsetzung sah eine Levi-Rückfrage vor, in Paket 6a nach Opus-Urteil entfernt).
4. (Frische-Rohdaten) `kerzen_seit_gegenseite_nas100/qqq` in DB (s.o. Punkt 1).
5. (9d TP1) `feedback_chartanalyse.md` Punkt 9d: Zonenhistorie-Prüfung gilt auch für das TP1-Level — Ablehnungen am TP1 müssen in der Entry-Freigabe-Zeile als "TP1-Double-Top-Risiko" erwähnt werden (KEIN Gate).
6. (9b Pflichtzeile) `feedback_live_trading.md` Punkt 2b (CronCreate-Block): `9b-Divergenz-Check: MACD-H(5min) [X] → [Y]` als eigene Pflichtzeile vor jeder Order verankert. Fehlen = Schritt 5 nicht abgeschlossen.
7. (Pivot-Distanz) `pivot_distanz_punkte` in DB (s.o. Punkt 1).

**Paket 6 — Opus-Endverifikation (27.08.2026): substanzieller Nacharbeitsbedarf, NICHT abgeschlossen.** DB-/Skript-Handwerk (Punkte 1, 4, 7 der Liste) verifiziert sauber — Schema-Kette lückenlos, Migration greift auf echter DB, kein Gate-Leak in status/ampel, Live-Test grün. Der Regelwerks-Teil hat vier Muss-Korrekturen:
1. **Levi-Rückfrage bei STALE muss raus** (`feedback_live_trading.md` 7b1c) — Opus-Urteil: unzulässige Auftragsüberschreitung, verändert Trade-Menge/-Timing entgegen der Analyse-Vorgabe ("Konsequenz im Anzeige-Modus: KEINE"), wirkt im Solo-Loop als vollständiges Veto. Genau der Punkt, den ich (Sonnet) vorab schon als klärungsbedürftig markiert hatte.
2. **Bedingung 4 der Analyse ("Struktur gegen Richtung", Zwei-Zweig) fehlt komplett** in 7b1c — durch Aufspaltung der Bedingung 1 wurde die Zahl 5 nur kosmetisch gehalten. Bedingung 4 ist laut Analyse der "Sicherungsstift" gegen den einzigen benannten False-Positive-Fall (Bärenflagge/Konsolidierung) — ohne sie feuert die Klasse dort mit.
3. **QQQ-Gate-Alter fälschlich auf 5min statt 15min** — dreifach konsistent falsch in `feedback_live_trading.md:493`, `trade_db.cjs:107`, `gate_check.cjs:722`, lockert die Bedingung um Faktor 3.
4. **Keine Befüllungspflicht für die neuen Felder** — DB-seitig fertig, aber keine Anweisung, die Werte beim Entry tatsächlich zu erheben/übergeben. Beleg: `adx_at_entry`/`trend_effizienz` (bestehende, strukturell gleich "optionale" Felder) stehen bei 0 von 43 Trades. Ohne Pflicht in 7b1 Schritt 5 + Punkt 2b bleibt Empfehlung 1 der Analyse ("höchste Priorität") operativ wirkungslos.

Kleinere Korrekturen: Review-/Verfallsklauseln für 7b1c (10-15 Fälle) und `pivot_distanz_punkte` (2-3 Fälle) fehlen; 9b-Pflichtzeile ohne "erkannt seit HH:MM"; Widerspruch 7b1a-Pflicht vs. bestehendes 7b1-Kommandotemplate; neue CLI-Flags fehlen im gate_check.cjs-Kopfkommentar/Batch-Doku, Namenskonvention weicht von `add_skipped_setup.cjs` (`--dg-*`) ab; Vorbedingung "&gt;6" vs. umgesetzt "≥6". Zusatzhinweis: Änderungen liegen noch uncommittet im Working Tree (anders als Paket 1-5).

**Status: Paket 6a UMGESETZT (27.08.2026, Fable direkt in der Hauptsession — Levi schaltete das Fenster per `/model` auf Fable, nachdem Fable zunächst nicht mehr im Tarif war und per Nutzungsguthaben reaktiviert wurde, siehe [[feedback_modellwahl_trading]]).** Alle 4 Muss-Korrekturen + 6 kleinere Punkte erledigt:
1. Levi-Rückfrage bei STALE entfernt — 7b1c-Konsequenz jetzt explizit "KEINE (Anzeige-Modus), keine Rückfrage", mit Opus-Begründung als Vermerk.
2. Bedingung 4 "Struktur gegen Richtung" (Zwei-Zweig, Short + spiegelbildlich Long) in 7b1c nachgetragen, Fünferliste wieder deckungsgleich mit der Analyse (Gate-Alter NAS100/QQQ wieder in EINER Bedingung 1), False-Positive-Begründung (Bärenflagge) dokumentiert.
3. QQQ-Gate-Alter auf 15min korrigiert an allen drei Stellen: `feedback_live_trading.md` 7b1c Bedingung 1, `trade_db.cjs` Spaltenkommentar `kerzen_seit_gegenseite_qqq`, `gate_check.cjs` printResult-Label (jetzt "NAS100(5min) / QQQ(15min)").
4. Befüllungspflicht verankert: 7b1 Schritt 5 neuer Absatz "Befüllungspflicht Schattenmess-Felder" (CLI-optional, aber protokollpflichtig — fehlender Wert nur mit explizit benanntem Grund) + Kurzfassung in Punkt 2b (8); Kommandotemplate Schritt 5 um `--impuls-reifegrad-atr`/`--impuls-reifegrad-atr-emaanker` erweitert (löst zugleich den 7b1a-Widerspruch).
Kleinere: Review-Pflicht 7b1c nach 10-15 Anwendungsfällen (Regelwerk) + Verfallsprüfung `pivot_distanz_punkte` nach 2-3 Fällen (Regelwerk + DB-/Skript-Kommentar); 9b-Zeile um "erkannt seit HH:MM" ergänzt; CLI-Flags vollständig im gate_check.cjs-Kopfkommentar inkl. Batch-camelCase-Feldern nachgetragen; `--dg-*`-Kurzformen aus add_skipped_setup.cjs als akzeptierte Aliasse in gate_check.cjs implementiert und dokumentiert (lange Form kanonisch); ">6" vs "≥6" auf ≥6 vereinheitlicht mit Vereinheitlichungs-Vermerk (Widerspruch lag schon in der Analyse selbst). Live-Test grün (Aliasse, Kerzen-Label, Pivot-Ausgabe; `node --check` beide Skripte sauber).

**Paket 6a — Opus-Endverifikation (27.08.2026): BESTANDEN (10/10 Punkte, unabhängige Dateiprüfung + Live-Tests).** Dabei 1 Muss-Punkt + 3 Kleinigkeiten als Nacharbeit gefunden → Paket 6b.

**Paket 6b (27.08.2026, Fable, direkt umgesetzt):**
1. (M1) Stale-Check-Zeile jetzt auch im tatsächlichen Punkt-2b-Pflichtbaustein verankert (direkt nach dem 9b-Block), konditional wie Basis-Reclaim: Zeile NUR bei erfüllter Vorbedingung, sonst ersatzloser Entfall; bei erfüllter Vorbedingung Pflicht mit "Schritt 5 nicht abgeschlossen"-Konsequenz.
2. (K1) Widerspruch in 7b1c aufgelöst: Ausgabevariante "keine Vorbedingung" gestrichen, Zeile erscheint ausschließlich bei erfüllter Vorbedingung (deckungsgleich mit Analyse Abschnitt 11 und der Basis-Reclaim-Konvention).
3. (K2) Veraltetes "Gate-Check ausgeführt:"-Kommandotemplate in 7b1 an das Schritt-5-Template angeglichen (beide jetzt inkl. `--impuls-reifegrad-atr`/`--impuls-reifegrad-atr-emaanker`).
4. (K3) `gate_check.cjs` printResult(): NAS100-/QQQ-Kerzenwerte unabhängig voneinander ausgegeben — vorher unterdrückte fehlendes `--kerzen-nas100` die komplette Zeile inkl. QQQ-Wert. Live-Test grün (nur-QQQ und beide).
Zusätzlich MEMORY.md-Zeile zur Testtag-Analyse ("noch NICHT umgesetzt") und die Paket-6-Kurzfassung (Item 3, alte Levi-Rückfrage) auf den aktuellen Stand gebracht.

**Paket 6b — Opus-Verifikation (27.08.2026): BESTANDEN** (alle 4 Punkte bestätigt, keine Regression). Dabei 2 kosmetische Restpunkte → **Paket 6c (27.08.2026, Fable, umgesetzt):** (1) `gate_check.cjs`-Kopfkommentar: Warnhinweis + vollständiges Live-Beispiel mit allen Paket-6-Parametern ergänzt, Verweis auf 7b1 Schritt 5 als verbindliche Fassung (die Minimal-Beispiele sind keine Live-Vorlage mehr). (2) Veralteter "aller 5 Pakete"-Zahlendreher in dieser Datei korrigiert. Keine weitere Opus-Runde nötig (reine Kosmetik).

**COMMIT AUSGEFÜHRT 27.08.2026 (Levi-Freigabe erteilt, von Sonnet direkt ausgeführt):** Commit `a1a19d8`, "Paket 6-6c: Dual-Gate-Schattenmessung auf trades, Stale-Klasse-Support, Frische-/Pivot-Rohwerte, K3-Ausgabefix (GEMESSEN, KEIN GATE)" — 2 Dateien (`scripts/gate_check.cjs`, `scripts/trade_db.cjs`), 161 Insertions/5 Deletions, Working Tree danach sauber. **Damit ist die gesamte Regelwerk-Überarbeitung (Pakete 1-6 inkl. 6a/6b/6c) abgeschlossen** — dreifach unabhängig von Opus verifiziert (Paket 1 einzeln, Paket 2+3 gemeinsam, Paket 6/6a/6b/6c in drei Durchgängen). Branch ist 1 Commit vor `origin/main`, noch nicht gepusht.
