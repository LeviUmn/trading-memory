---
name: project_regelwerk_entscheidung_2026-09-07
description: "Fable-Umsetzung der Opus-Auftragsliste 07.09.2026 ([[project_fable_auftragsliste_2026-09-07]]) in ZWEI Runden. Runde 1: #1a/#1b, #2a/#2b, #5, #6a/b/c, #7, #8-Werkzeug, Anhang A — 5 neue Skripte, gate_check.cjs umgebaut, 4 Regelwerksdateien. Runde 2 nach Opus-Gegencheck ([[project_gegencheck_fable_umsetzung_2026-09-07]]): O1-Fix, E1 Option C im Code, E3 'SL-Anker-Vorprüfung' (Wick-Zonen-Registerkategorie + gate_check --sl-vorpruefung + Regeltext), E4/E5 als entschieden, #9 nachgetragen (atr_at_entry 38,4/42,3 in trades.db/trades.sql), E2-Kurswechsel dokumentiert (Opus: Sockel bleibt), E6/E7 neu gefasst. Runde 3 (nach zweitem Gegencheck, alle 6 R2-Punkte OK): N1 leeres Wick-Set → NICHT PRÜFBAR statt tauglich, N2 Skipped-Setup-Pflicht + Bilanz-Kategorie '2/2 ohne 7b1', E6-Quotenbänder im Batch (Nenner Trigger, --handelstage), N3 'entschieden' → 'Opus-Empfehlung, Levi-Bestätigung ausstehend'. Laut Opus danach entscheidungsreif; offen nur #8-Datenlauf (eigene Session) und Levis Entscheidung. NICHT committet, MEMORY.md nicht angefasst."
metadata:
  node_type: memory
  type: project
  status: runde3_umgesetzt_entscheidungsreif_fuer_levi
  originSessionId: b594a3be-34a1-46da-8616-0f50ceb56daa
  modified: 2026-09-07T16:43:42.390Z
---

# Fable-Umsetzung der Opus-Auftragsliste 07.09.2026 (Testtag-Analyse 04.09.) — Runde 1, 2 und 3

Quelle: [[project_fable_auftragsliste_2026-09-07]] (10 Punkte + Anhang A, Opus mit Codeverifikation). Gegencheck nach Runde 1: [[project_gegencheck_fable_umsetzung_2026-09-07]] (12/12 OK, 1 Korrektur O1, 5/6 Fable-Gegenbefunde bestätigt, 1 widerlegt). Rollen: Fable setzt um und bereitet vor, Levi entscheidet ([[feedback_modellwahl_trading]]). Vorbild: [[project_regelwerk_entscheidung_2026-08-31]]. **Alle Repo-Änderungen sind UNCOMMITTET** (`git status`: `M .gitignore`, `M scripts/gate_check.cjs`, `M scripts/trades.sql`, 5 neue `scripts/*.cjs` untracked, `memory/testtag/...` im untracked `memory/`); Commit nach Levi-/Sonnet-Prüfung. Regelwerksdateien im Memory-Verzeichnis direkt geändert.

## Statusübersicht (Stand nach Runde 2)

| Punkt | Kategorie | Status |
|---|---|---|
| #1a TP2-Kandidaten-Zeile | SKRIPT-FIX | UMGESETZT R1 — `gate_check.cjs` Block "Punkt-1a (07.09.2026): TP2-KANDIDATEN", Ausgabe `TP2-Kandidaten (Anzeige, kein Gate)` |
| #1b `--tp2-level-price` A3-Pflicht + `none` | SKRIPT-FIX | UMGESETZT R1 — A3-Liste (`{ key: 'tp2-level-price' }`), `--grund-tp2-level-price` |
| #1c Rechtsfolge belegtes `none` | REGELWERK-FRAGE | **UMGESETZT R2 als Option C — VON LEVI BESTÄTIGT 07.09.2026**: `none` + kein Registerlevel ≥RR2 → `pass:true` + `sizingFlag 'halbe Position'` (Grund "TP2: kein RR>=2-Level im Register (8b2, none belegt)"); `none` + Kandidat → `pass:null`/UNKLAR "Beleg widerlegt"; Register nicht geladen (Batch ohne strikt) → UNKLAR |
| #2a Zonenband + Mehrfachzonen | SKRIPT-FIX | UMGESETZT R1 — `parseClusterZones()`, Gate 4; R2 zusätzlich `--cluster-level register` (Wick-Zonen aus dem Register) + WICK-ZONEN-WARNUNG bei Auslassung/`none`-Widerspruch |
| #2b 8c2-Gegenrechnung | SKRIPT-FIX | UMGESETZT R1 — reproduziert 18:49: konformer SL ≤ 29487,65 → Fenster 4,0 Pkt, KEIN Level |
| #3 30-Pkt-Sockel | REGELWERK-FRAGE | **KURSWECHSEL dokumentiert (R2)**: Opus empfiehlt jetzt Option D (nichts ändern, schließen) — vermerkt in der Auftragsliste #3, im Code-Kommentar an `CLUSTER_TRIGGER_SOCKEL` und hier (E2); Entscheidung bei Levi; `--experiment ohne-sockel` bleibt Messinstrument |
| #4 P4 vs. 8c2 | REGELWERK-FRAGE | **UMGESETZT R2 als "SL-Anker-Vorprüfung" — VON LEVI BESTÄTIGT 07.09.2026, BINDEND als 7b1 Schritt 0 (Finalisierung unten)**: (a) Registerkategorie `Wick-Zone (8c2)` mit `tief`/`hoch` via `register_touch.cjs --wick-zone`, (b) `gate_check.cjs --sl-vorpruefung` (Anzeige, kein A3, Exit 0), (c) Regeltext [[feedback_live_trading]] Item (6f) + [[feedback_vollcheck_format]] Template-Zeile + Absatz. Bandlogik und Alters-/Impulskriterium verworfen (Auftragsliste #4 vermerkt) |
| #5a drei Zahlenkorrekturen | PROZESS | UMGESETZT R1 — Nachtrag am Ende von `memory/testtag/testtag_2026-09-04.md` |
| #5b maschinelle Bilanz | SKRIPT-FIX | UMGESETZT R1 — `scripts/protokoll_bilanz.cjs`; **R2 O1-Fix**: Register-Updates "nicht messbar (Log beginnt am …)" statt Fehlalarm, wenn der Touch-Log jünger ist als das Protokoll |
| #6a Wanduhr-Deadline | SKRIPT-FIX | UMGESETZT R1; **E4 ENTSCHIEDEN R2: 12 Min** — Konstante `RETEST_ZEITBOX_MINUTES = 12` mit Entscheidungsvermerk, Ausgabetext "E4 entschieden 07.09.2026", Regeltext angepasst |
| #6b/#6c Wiederholungssperre / Kollision | SKRIPT-FIX | UMGESETZT R1 — `scripts/last_gate_fail.json` (gitignored), Zeile `GATE-GEDAECHTNIS` |
| #7 Fib-Extension | SKRIPT-FIX | UMGESETZT R1 — `register_touch.cjs --fib-*`; **E5 ENTSCHIEDEN R2: Faktor 2,0 NICHT** (Kopfkommentar register_touch, [[feedback_session_update]] Schritt 6), Standard 1.272/1.618 + Measured Move |
| #8 Machbarkeitsmessung | DATENTASK | WERKZEUG R1 (`--batch-strikt`, `--experiment`, Bindungsstatistik, Skipped-Setup-Aufruf); **Datenlauf NICHT in dieser Runde** — machbar per `replay_start`-Zyklen (O3), eigene Session; E6-Bänder neu (unten) |
| #9 ATR(5min) 21.08. | DATENTASK | **ERLEDIGT R2**: Opus maß #42 = 38,4 / #43 = 42,3 Pkt; `atr_at_entry` per `updateGateResult` in `trades.db` geschrieben, `trades.sql` per `dump_trades_sql.cjs` neu erzeugt (Diff: nur diese zwei Werte + seit dem letzten Dump migrierte Spalten `adx_at_entry`/`dual_gate_*`/`kerzen_*`/`pivot_distanz_punkte` — Dump war veraltet, 43 INSERTs unverändert bis auf #42/#43) |
| #10 1H-Asymmetrie messen | PROMPT/PROZESS | UMGESETZT R1; **E7 R2 nachgetragen**: Blockade zählt erst ab ≥15 Min UND ≥1,5×ATR, kontrafaktisches Ergebnis pro Blockade (beidseitig) — [[feedback_vollcheck_format]] |
| Anhang A Register-Frische | bindend | UMGESETZT R1 — `register_constants.cjs`, `register_check.cjs` (Exit 0/1/2), `register_touch.cjs`; Regeltext in 4 Dateien |

## Runde 1 — was umgesetzt wurde (Kurzfassung, Details siehe Skript-Kopfkommentare)

**Anhang A:** gemeinsame Schwellenquelle 60/90/2,0; `register_check.cjs` liefert die Pflichtzeile `Register-Check: OK|WARNUNG|HART …` (gegen das echte Register vom 04.09. korrekt HART, 4000+ Min); `register_touch.cjs` ist der einzige `updated`-Schreibweg (echte UTC, Zukunftsstempel Exit 1, Log `register_touch_log.jsonl`) und pflegt Rundzahl-Band, Fib/MM, Session-Extrema. **#1a/#1b, #2a/#2b, #6a/b/c, #7, #8-Werkzeug:** alles in `gate_check.cjs`, Kommentarblock "OPUS-AUFTRAGSLISTE 07.09.2026" bei den Konstanten. **#5b:** `protokoll_bilanz.cjs` zählt Voll-Checks (beide Kopfzeilenformate), gate_check-Aufrufe mit `--chasing`/`--tp2`, Screenshots, Register-Updates, gleicht gegen die Behauptungen ab. **Regelwerk:** [[feedback_vollcheck_format]] (Register-Check-, 1H-Schatten-Zeile), [[feedback_live_trading]] ((6c) ergänzt, (6d)/(6e) neu, Punkt 9), [[feedback_tagesabschluss]] (Bilanz maschinell), [[feedback_session_update]] (Schreibweg). `.gitignore`: drei neue Laufzeitdateien.

**Kontrafaktum bestätigt:** 18:49-Lauf mit `--batch-strikt --experiment ohne-sockel` → PASS. **Bandlogik-Widerspruch belegt** (Test 8): #42 halbiert, 04.09. bleibt FAIL — Opus hat das im Gegencheck bestätigt und Option C zurückgezogen.

## Runde 2 — Umsetzung der Gegencheck-Vorgaben

1. **O1** `protokoll_bilanz.cjs`: Touch-Log ohne Einträge zum Protokolldatum + ältester Eintrag jünger als das Datum → `Register-Updates: nicht messbar (Log beginnt am 2026-09-07, Protokoll ist aelter — O1)`, keine Abweichung. Getestet: Log vom 07.09. gegen Protokoll 04.09. → nicht messbar, Exit 1 nur noch wegen der zwei abgekürzten Aufrufe; Log mit 1 Eintrag am 04.09. → "behauptet 4, gezählt 1" (korrekt).
2. **E1 Option C** in `gate_check.cjs` (Zuweisung im TP2-Kandidaten-Block, weil dort die Kandidatenprüfung liegt): Test "none mit 9 Kandidaten" → `[N/A] … Beleg WIDERLEGT -> UNKLAR`; Test "none ohne Kandidaten" (Entry 29900) → `[PASS ✓] … Sizing-Signal "halbe Position"`, Sizing-Flag gesetzt. Halbierungsgrund eigenständig benannt (nicht mehr "<2:1").
3. **E3 SL-Anker-Vorprüfung:** `register_touch.cjs --wick-zone "29499.35/29240-29254" --wick-zone-quelle "…"` legt Einträge `{type:"Wick-Zone …", levelart:"Wick-Zone (8c2)", tief, hoch, abrufweg}` ohne `price` an (nie TP-Kandidat; `eligibleLevels` filtert sie). `gate_check.cjs`: `wickZonesFromRegister()`, `--cluster-level register` (leer = wie `none` mit Kennzeichnung), Warnung bei numerischer Zonenliste, die eine Register-Zone auslässt, oder `none` trotz Register-Zonen; Statuszeile weist Wick-Zonen aus. Neuer Modus `--sl-vorpruefung --entry --sl --dir [--atr --tp1 --tier --cluster-level --level-register]` → `runSlVorpruefung()`: 8c2- und 8c-Zeile, Gegenrechnung, TP1-Fenster mit diesem SL, `URTEIL: SL-ANKER TAUGLICH / DERZEIT UNTAUGLICH / VORAUSSICHTLICH TAUGLICH (nicht prüfbar)`, Exit immer 0, kein A3, kein DB-Write, kein Gate-Gedächtnis. Tests: 18:49-Anker → UNTAUGLICH (8c2), SL 29480 → TAUGLICH. Regeltext (Levi-Bestätigung ausstehend): [[feedback_live_trading]] Item (6f) mit Rechtsfolge "Setup ohne tauglichen SL-Anker löst keinen 7b1-Ablauf aus — als '2/2-Zustand, SL-Anker untauglich (8c2), kein 7b1' protokollieren und zählen", Punkt-9-Absatz "Wick-Zonen gehören ins Register"; [[feedback_vollcheck_format]] Template-Zeile `SL-Anker-Vorprüfung (8c2)` (nur bei beobachtetem Setup) + Absatz; [[feedback_session_update]] Schritt 6 (`--wick-zone`).
4. **E4/E5 festgeschrieben:** `RETEST_ZEITBOX_MINUTES = 12` mit "ENTSCHIEDEN"-Kommentar, RETEST-Zeile ohne "offen"; Fib 2,0 "NICHT" in `register_touch.cjs`-Kopf und [[feedback_session_update]]; [[feedback_live_trading]] (6c) "12 Min, E4 entschieden".
5. **#9 nachgetragen:** `trades.db` #42 `atr_at_entry` NULL → 38,4, #43 NULL → 42,3 (per `updateGateResult`, keine anderen Felder); `scripts/trades.sql` neu gedumpt (siehe Statustabelle zur Diff-Erklärung). Damit ist O2 auch in der DB belegt: SL 53 Pkt = 1,25-1,38×ATR, unter dem normalen 1,5×-Floor.
6. **E2 Kurswechsel dokumentiert:** Auftragsliste #3 (neuer Absatz "KURSWECHSEL 07.09.2026"), #4 (Option C zurückgezogen, NEU E), Code-Kommentar an `CLUSTER_TRIGGER_SOCKEL`. Nichts implementiert — Entscheidung bei Levi, Empfehlungsrichtung jetzt D.
7. **E6/E7 nachgetragen:** E6-Bänder neu (Nenner = Trigger-Kandidaten; <10 % zu eng / 10-35 % Leveldichte+Timing / >35 % Trigger-Timing; Sekundärmaß 0,3-1,0 PASS/Handelstag; Kalibrierung nur für das Gate mit >50 % Alleinblocker-Anteil UND beide Maße unter Band) — in Runde 2 nur dokumentiert, **in Runde 3 im Batch-Code umgesetzt** (siehe unten). E7 (≥15 Min UND ≥1,5×ATR, kontrafaktisch beidseitig) in [[feedback_vollcheck_format]] eingetragen.

## Runde 3 — vier Fixes nach dem zweiten Opus-Gegencheck (alle R2-Punkte bestätigt OK)

1. **N1 — kein Gummistempel bei leerem Register:** `--cluster-level register` ohne eingetragene Wick-Zone → 8c2 `pass:null` (UNKNOWN) mit Semantikzeile "NICHT ABSCHLIESSEND PRUEFBAR … Abwesenheit ist mit --cluster-level none zu belegen, nicht durch ein leeres Register". `--sl-vorpruefung` hat jetzt drei Urteile: TAUGLICH (nur 8c2 PASS und 8c nicht FAIL) / UNTAUGLICH (ein FAIL) / NICHT ABSCHLIESSEND PRÜFBAR (8c2 oder 8c `null` — leeres Wick-Set, ATR fehlt). Getestet: Register-Kopie ohne Wick-Zonen → Vorprüfung NICHT PRÜFBAR, Live-Gate UNKNOWN (Exit 2); ohne `--atr` → NICHT PRÜFBAR.
2. **N2 — kein Statistik-Loch:** UNTAUGLICH druckt den fertigen `add_skipped_setup.cjs`-Aufruf mit Grund "SL-Anker untauglich (…) — 2/2-Zustand ohne 7b1, kein gate_check-Aufruf" (Pflicht). `protokoll_bilanz.cjs` zählt die Kategorie "2/2-Zustand ohne 7b1 (SL-Anker untauglich)" dreifach (UNTAUGLICH-Urteile / protokollierte "2/2-Zustand … kein 7b1"-Zeilen / Skipped-Setup-Aufrufe) und meldet als Abweichung, wenn Urteile > Aufrufe (getestet mit synthetischem Protokoll: 1/1/0 → Abweichung, Exit 1). Regeltext in [[feedback_live_trading]] (6f) und [[feedback_tagesabschluss]] Unterpunkt 1 ergänzt.
3. **E6-Quotenbänder im Batch:** Nenner = Trigger-Kandidaten (Batch-Feld `triggerId`; mehrere Datensätze desselben Triggers = SL-/TP-Varianten, Trigger ist PASS-fähig, sobald ein Datensatz PASS; ohne `triggerId` = ein Datensatz pro Trigger). Bänder <10 % zu eng / 10-35 % Leveldichte+Timing / >35 % Trigger ist der Engpass; `--handelstage <n>` liefert das Sekundärmaß PASS/Handelstag mit Zielkorridor 0,3-1,0; Vorab-Bindung als Zeile. Getestet: 3 Datensätze, 2 Trigger, 1 PASS-fähig → 50 %, 0,5/Tag "im Band".
4. **N3 — Formulierung:** alle "#1c entschieden 07.09.2026"-Texte in `gate_check.cjs` (Ausgabe und Kommentare) auf "nach Opus-Empfehlung 07.09.2026, Levi-Bestaetigung ausstehend" umgestellt; Kopfkommentar entsprechend. Auftragsliste Punkt 3 Option C trägt jetzt den Inline-Vermerk "ZURÜCKGEZOGEN 07.09.2026" (Punkt 4 hatte ihn bereits).

## Finalisierung nach Levis Bestätigung (07.09.2026 abends)

Levi hat alle vier offenen Punkte explizit bestätigt (Opus' Empfehlungen aus dem dritten Gegencheck): **#1c Option C** (Code lief bereits so), **#3 Option D** (30-Pkt-Sockel bleibt, Frage geschlossen), **#4 SL-Anker-Vorprüfung — Rechtsfolge "kein 7b1-Ablauf bei untauglichem SL-Anker" wird bindend**, **E4/E5** (Zeitbox 12 Min, kein Fib 2,0). Umgesetzt:
- **Platzhalter final formuliert** (gezielter Grep nach "Levi-Bestaetigung ausstehend / Opus-Empfehlung / OFFEN / steht aus / 12 vs. 15"): `gate_check.cjs` (Kopfkommentar, Konstantenblock "ENTSCHIEDEN VON LEVI", `CLUSTER_TRIGGER_SOCKEL`-Kommentar, `RETEST_ZEITBOX_MINUTES`, TP2-none-Ausgabetexte "von Levi bestaetigt 07.09.2026", 8c2-Kommentar, A3-Listenkommentar, `runSlVorpruefung`-Kopf/Header-Zeile/URTEIL-Texte "7b1 SCHRITT 0, bindend seit 07.09.2026"), `register_touch.cjs` (E5), `protokoll_bilanz.cjs` (Kategorie "2/2 ohne 7b1" als ERWARTETER Normalfall bei UNTAUGLICH, Soll: Urteile = protokollierte Zeilen = Skipped-Setups; zweite Abweichungsmeldung, wenn die Ausschreibung im Voll-Check fehlt). Regelwerk: [[feedback_live_trading]] (6f) "von Levi bestätigt, BINDEND als 7b1 Schritt 0", [[feedback_vollcheck_format]] (SL-Anker-Absatz, E7-Schwelle), [[feedback_session_update]] (E5), Auftragsliste #1c/#3/#4 mit "VON LEVI BESTÄTIGT".
- **#4 als verbindlicher Prozessschritt:** neuer **Schritt 0** im 7b1-Fünf-Schritte-Ablauf ([[feedback_live_trading]] "Ab jetzt verbindlich") und Schritt **8b** im Voll-Check-Ablauf ([[feedback_loop_ablauf_uebersicht]]): bei jedem 2/2-Trigger `--sl-vorpruefung` ZUERST; UNTAUGLICH → kein 7b1, `2/2-Zustand, SL-Anker untauglich (8c2), kein 7b1` + `add_skipped_setup.cjs`-Pflicht; TAUGLICH → Schritte 1-5; NICHT ABSCHLIESSEND PRÜFBAR → nur mit belegtem `--cluster-level none` frei. **Bewusst kein neuer Hard-Gate-Code** — der Anzeige-Modus bleibt Exit 0 ohne A3; die Bindung ist Prozess der Loop-Session, die Zählung liegt bei `protokoll_bilanz.cjs`.
- Keine Verhaltensänderung im Live-Gate-Pfad gegenüber Commit `eb7caeb` (nur Kommentare/Ausgabetexte/Bilanz-Meldung); `node --check` und Funktionstest der Vorprüfung nach den Textänderungen ok.
- **Commits (lokal, nicht gepusht):** `eb7caeb` — Runden 1-3 (8 Dateien, +1621/−101, "keine Handelsregel live aktiviert"); `c72a836` — Finalisierung nach Levi-Bestätigung (gate_check.cjs, protokoll_bilanz.cjs, register_touch.cjs; +43/−27; SL-Vorprüfungs-Rechtsfolge bewusst aktiv). Nicht committet, wie verlangt: `scripts/trade_stats.cjs` (fremde 82 Zeilen), `memory/` (Auto-Memory, nicht Teil des Repos). Regelwerksdateien liegen im Memory-Verzeichnis (kein Git).

## Offen (nach Finalisierung)

- **Nur noch Datenläufe:** #8-Machbarkeitsmessung (eigene Session, `replay_start`-Zyklen, `triggerId`, `--handelstage`) und ggf. #9-Vertiefung. Keine offene Regelentscheidung mehr.
- **#8-Datenlauf:** eigene Session, ~12 `replay_start`-Zyklen für 20 Handelstage (O3-Workaround), Register-Snapshots pro Tag (`--level-register`), Batch-JSON mit `triggerId` je Trigger-Kandidat, vier kontrafaktische Durchgänge (`--experiment`), Auswertung mit `--handelstage <n>`.
- **Nicht angefasst:** Anhang B der Auftragsliste; `scripts/trade_stats.cjs` (fremde uncommittete Änderungen von vor dieser Runde).

## Befunde dieser beiden Runden (ungeglättet)

1. Option C zu #3/#4 war geometrisch inkohärent — von Opus bestätigt, zurückgezogen, durch E3 ersetzt.
2. Opus' "ATR ≤ 21"-Ableitung war falsch; real 38,4/42,3 → #42/#43 lagen unter dem normalen 1,5×-Floor. F4(3) ist damit widerlegt, der Sockel war am Ursprungstag die bindende Regel.
3. Zwei "wörtliche" gate_check-Aufrufe am 04.09. abgekürzt (Zeilen 1743/1805); stiller Kopfzeilen-Formatwechsel ab VC#49 — beide von Opus bestätigt.
4. Mein Gegenbefund "Datenzugang strukturell blockiert" war FALSCH — `ui_scroll left` lädt Historie nach, `replay_start <Datum>` liefert die Bars davor. `chart_scroll_to_date`/`set_visible_range` tun es nicht; das bleibt als Werkzeug-Eigenschaft notiert (Kopfkommentar `atr_from_ohlcv.cjs` nennt noch nur den Scroll-Weg — beim nächsten Anfassen um `replay_start` ergänzen).
5. `trades.sql` war seit den Paket-6-Spalten nicht neu gedumpt worden — der Dump-Schritt aus dem Kopfkommentar von `dump_trades_sql.cjs` ("nach jeder gate_check-Rückschreibung") wurde offenbar nicht gelebt.

## Testnachweise (07.09.2026, Testkopie des Registers im Scratchpad; echtes Register, echte DB nur für #9 berührt)

Runde 1: Live 18:49 skalar/none → FAIL, Kandidaten, Gegenrechnung, Deadline, #6b, Skipped-Setup; Band+Mehrfach → Kollision; Wiederholung → WIEDERHOLUNGSAUFRUF; tp2 fehlt → Exit 1; Experiment live → Exit 1; Batch strikt ohne-sockel → 18:49 PASS; bandlogik → #42 halbe/UNKNOWN, #43 FAIL; Batch normal 0/3/0; BOM ok; `register_check` HART; `register_touch` Zukunft → Exit 1; Bilanz 55 VCs. Runde 2: E1 beide Zweige; `--wick-zone` (2 Zonen); `--cluster-level register` → FAIL an 29499,35, Register-Statuszeile nennt 2 Wick-Zonen; numerisch ohne Register-Zone → WICK-ZONEN-WARNUNG; `--sl-vorpruefung` UNTAUGLICH (18:49) / TAUGLICH (SL 29480), Exit 0; O1 beide Fälle; `node --check` aller Skripte ok; `last_gate_fail.json` nach den Tests entfernt.
