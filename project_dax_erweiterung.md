---
name: dax-erweiterung
description: "Ausbau DAX von reiner Papier-Beobachtung zu eigenständigem Vormittags-Trading-Block (10-15 Uhr), parallel zu NAS100 (15:30-22 Uhr) — Entscheidungen, Architektur, To-do-Liste"
metadata:
  type: project
  originSessionId: session-2026-08-05
  modified: 2026-08-07T12:10:21.113Z
---

Levi will das System am 05.08.2026 um einen eigenständigen DAX-Trading-Block erweitern: **10-15 Uhr DAX** (eigener Trigger, seit 07.08.2026 **"start update dich dax"**, ursprünglich als "#1" geplant), danach **ab 15 Uhr NAS100** wie bisher (bleibt schlicht **"start update dich"**, ursprünglich zusätzlich als "#2" geplant — Umbenennung auf Levis Wunsch für mehr Eindeutigkeit). Das ist ein Strategiewechsel gegenüber der bisherigen Festlegung in [[feedback_instrumenten_fokus]] (DAX bisher nur situatives Ergänzungsinstrument, nicht paralleler Tagesblock mit eigenem Kapital) — bewusst von Levi entschieden, nicht implizit übernommen.

**Why:** Levi-Zitat 05.08.2026: "Ziel für mich langfristig ist, dass wir ab 10 Uhr morgens bereits auf den Dax traden, genauso simultan wie wir es auf dem Nasdaq machen." Begründung für die Datenerweiterung: "Die Daten die wir täglich ziehen sind ja schon bereits wichtig auch für den Dax, weil USA regiert die Märkte weltweit" — US-Makrodaten bleiben relevant, EZB/deutsche Daten kommen als DAX-spezifische Ergänzung hinzu.

## Fable-Review (05.08.2026) — Kernfunde

Volles Review lief als Hintergrund-Agent, hier nur die Punkte mit Konsequenz für die Umsetzung:

- **Strukturelle Lücke entdeckt, nicht nur inhaltliche:** `scripts/trade_db.cjs` hat keine `symbol`/`instrument`-Spalte im Schema — die Tabelle kann zwei Instrumente nicht sauber unterscheiden. `scripts/cooldown_check.cjs` hat `LOG_PATH` hart auf `memory/trades/trade_log.md` verdrahtet (nur per Env-Var `TRADE_LOG_PATH` überschreibbar). Bisherige DAX/NAS100-Trennung war reine Disziplin (Levi hat DAX-Notizen bewusst in `dax_beobachtung/` gehalten), nicht strukturell erzwungen — das reicht laut Fable nicht mehr, sobald echtes DAX-Kapital dazukommt. Bezug zu bekannten Vorfällen: genau dieser Fehlertyp (Regel lebt nur im Memory, bricht unter Zeitdruck) verursachte bereits den Cooldown-Bruch bei Trade #12 und den size.cjs-Short-SL-Bug.
- **DAX-Volumen-Proxy ist unverifiziert:** Ob `XETR:DAX` (echter Xetra-Kassaindex-Feed) dasselbe Volumen-Problem hat wie `FOREXCOM:NAS100` (CFD, nachweislich kein Volumen) ist NICHT geprüft — vor jeder Planung eines QQQ-Äquivalents für DAX erst live testen (`data_get_study_values` mit Volume-Indikator auf DAX).
- **Loop-Mechanik (CronCreate, Modulo-5-Weiche, 5-Tool-Sequenz) ist 1:1 übertragbar** — symbol-agnostische Tooling-Infrastruktur. **Inhaltliche Kalibrierung ist es NICHT:** ATR-Schwellen/Rausch-Kerzen-Mindestdistanz, Chop-Zeitfenster (15:30-16:00-Regel) und die ganze Entscheidungsbaum-/Stall-Logik wurden über Monate speziell an NAS100-Verhalten nachgeschärft (z.B. ATR-Wahl nach Trade #21). Für DAX gilt das erstmal als unverifizierte Annahme, nicht als übernommene Wahrheit.
- **Chart-Layout-Engpass:** 5-Indikatoren-Limit/Chart gilt weiterhin. DAX-Block braucht ein eigenes, separat gespeichertes Layout (analog NAS100+QQQ-2v-Split), das per `layout_switch` beim 15-Uhr-Übergang gewechselt wird — kein Live-Umbau des bestehenden Layouts (fehleranfällig, kostet Zeit genau im Übergangsmoment).
- **EZB-Daten über FRED sind Sekundärquelle:** FRED führt Euroraum-Serien (ECB-Sätze, HICP), aber verzögert gegenüber EZB SDW/eurostat direkt. Für den strukturellen Hintergrund-Zweck (wie FRED es für USA auch nur ist) als pragmatischer erster Schritt ausreichend, aber keine gleichwertige Primärquelle — bei Bedarf später auf echte EZB-Quelle upgraden.

## Levis Entscheidungen (05.08.2026)

| Frage | Entscheidung |
|---|---|
| Kapitalbudget | DAX bekommt vorerst ein **eigenes, separates Budget** (Betrag noch festzulegen). Sobald NAS100 Phase 4 abgeschlossen ist, verschmilzt das zu einem **gemeinsamen 50.000€-Gesamtpool für alle Märkte** (DAX + NAS100) — das war schon länger als Zielbild für NAS100 allein geplant (siehe [[project_risikomanagement]] Skalierungs-Fahrplan), jetzt explizit auf DAX erweitert. Bis dahin läuft DAX NICHT im bestehenden 15.000€-NAS100-Pool mit. |
| Phasenlogik | **Eigene Phase 1 von vorne**, analog zum NAS100-Start (kleine Positionen, Bewährungsfenster) — trotz vorhandener Erfahrung/Regelwerk, weil Marktverhalten/Level/Setups auf DAX unbewiesen sind. Keine automatische Übernahme des NAS100-Phasenstands. |
| Overlap 15 Uhr | **DAX-Position wird vor Start von "start update dich" (NAS100) geschlossen.** Kein paralleles Management zweier offener Positionen/Loops gleichzeitig — bewusst die sicherere Variante, auch wenn das den Nasdaq-Einstieg situativ verzögern kann. |
| Korrelationsregel | Die bestehende Regel aus [[feedback_instrumenten_fokus]] ("nie gleichzeitig NAS100 und DAX in dieselbe Richtung") **bleibt vollständig gültig** — war makro-, nicht zeitlich begründet (beide reagieren auf dieselben globalen Katalysatoren), besonders relevant im 15-15:30-Uhr-Übergangsfenster. |
| Trades/Tag-Limit | **Gemeinsame Obergrenze** über DAX+NAS100 zusammen (3 regulär, 4. Ausnahme, 5. nie) — keine getrennte Zählung pro Markt, um die Gesamt-Exposure/Entscheidungsdichte auf dem bisherigen Niveau zu halten. |

## Architektur-Entscheidung: strukturelle statt disziplinbasierte Trennung

Nach Fables Empfehlung — physische Dateitrennung statt gemeinsamer Tabelle mit Instrument-Spalte-Filter (zu fehleranfällig):

- Eigene DB: `scripts/trades_dax.db` + eigenes Modul `trade_db_dax.cjs` (Kopie der Struktur aus `trade_db.cjs`)
- Eigenes Trade-Log: `memory/dax_trades/trade_log_dax.md`, `cooldown_check.cjs` für DAX über die bereits vorhandene `TRADE_LOG_PATH`-Env-Var auf diese Datei zeigen lassen
- Neuer Ordner `memory/dax_trades/` für echte DAX-Trades (ersetzt/ergänzt `dax_beobachtung/`, das explizit als "Papier, kein Kapital" benannt war — bei echtem Kapital ist der alte Name irreführend)
- Eigenständiges `project_risikomanagement_dax.md` (kein Anhang ans bestehende Dokument)
- Eigenständiges `feedback_session_update_dax.md` mit eigenem Trigger "start update dich dax" (umbenannt 07.08.2026, vorher "#1")
- Eigenständiges `feedback_live_trading_dax.md` — Mechanik-Teile aus [[feedback_live_trading]] übernehmen, inhaltliche Schwellen explizit als "unverifiziert, in Bewährung" markieren
- `add_trade.cjs`/`trade_stats.cjs`/`migrate_trade_log.cjs`: eigene DAX-Pendants statt Parameter-Flag (robuster gegen Vergessen bei dieser Codebasis-Größe)

Siehe [[feedback_dax_trennung]] für die fortgeschriebene Trennungsregel.

## Timing-Klarstellung (05.08.2026, direkt im Anschluss)

**DAX-Live-Trading (echtes Kapital) startet erst, wenn NAS100 alle Phasen (1-4) durchlaufen hat.** Bis dahin läuft nur der Infrastruktur-Aufbau (To-do-Liste unten) parallel zum normalen NAS100-Live-Trading — kein DAX-Kapitaleinsatz vor Abschluss von NAS100 Phase 4. Levi-Zitat: "Da müssen wir ja erstmal nachhaltig durch die Phasen [beim Nasdaq]. Das andere bauen wir dann parallel auf und starten dann mit dem Dax Trading erst, wenn wir beim Nasdaq durch alle Phasen sind."

**Konsequenz für die Kapitalbudget-Zeile oben:** Die Frage nach einem separaten DAX-Startbudget (To-do 3) entschärft sich dadurch — DAX startet ohnehin erst zeitlich nach NAS100 Phase 4, also potenziell direkt in der Nähe des ohnehin geplanten gemeinsamen 50.000€-Gesamtpools, statt vorher ein eigenes Zwischenbudget zu brauchen. Bei Erreichen von NAS100 Phase 4 erneut prüfen, ob überhaupt noch ein separates Vorab-Budget nötig ist oder ob DAX direkt in den dann vorhandenen Gesamtpool startet (mit eigener Phase-1-Positionsgröße gemäß der bereits getroffenen Entscheidung).

**Was bis dahin weiterläuft:** Infrastruktur-Aufbau (To-do-Liste, technische Trennung, Datenquellen, Chart-Layout) UND fortgesetzte Paper-Beobachtung in `dax_beobachtung/` zur Schwellen-Kalibrierung (To-do 13) — beides parallel zum normalen NAS100-Trading, ohne den NAS100-Fokus zu verwässern.

## To-do-Liste (priorisiert)

**Zuerst (vor jeder Code-Änderung):**
1. ✅ **ERLEDIGT 06.08.2026:** Live-Check `XETR:DAX` Volumen — bestätigt ECHT (146K-526K/5-Min-Bar, variierend). ABER: `chart_get_state` löst `XETR:DAX` intern zu `XETR_DLY:DAX` auf — Levi bestätigt live, dass das ein **15-Minuten-Delay** ist (per Timestamp-Vergleich verifiziert: letzter Bar bei `XETR:DAX` lag exakt 900s/15min hinter Echtzeit-Symbolen zurück). `XETR:DAX` damit NICHT geeignet fürs Live-Trading trotz echtem Volumen.
2. ✅ **ERLEDIGT 06.08.2026 (erweitert um Symbolwahl statt reinem Proxy):** Alternativen live getestet (`symbol_search` + `chart_get_state`/`data_get_ohlcv`/`quote_get` je Kandidat):
   - `FOREXCOM:GER40` (NAS100-Pendant): kein Delay-Suffix, aber Volumen durchgängig 0 — gleiches Problem wie `FOREXCOM:NAS100`.
   - **`IG:DAX` ("Germany 40 Cash") — GEWÄHLT:** kein Delay-Suffix (Bars laufen bis zum aktuellen Zeitstempel, kein Rückstand), UND echtes variierendes Volumen (67-351/5-Min-Bar). Preis trackt `XETR:DAX` eng (~10 Punkte/0,04% Abweichung, konsistent mit dessen 15-Min-Delay, kein struktureller Bruch).
   - **Entscheidung: `IG:DAX` wird das DAX-Chartsymbol** — Echtzeit und echtes Volumen in einem Symbol, kein separates Proxy-Instrument nötig (anders als NAS100/QQQ-Konstruktion). Alle folgenden To-dos (Layout, Risikomanagement-Doc, etc.) beziehen sich auf `IG:DAX`, nicht `XETR:DAX`.
3. Konkreten €-Betrag für das separate DAX-Startbudget festlegen

**Nachtrag 06.08.2026 — Volumen-Validierung `IG:DAX`:** Auf Levis Nachfrage 15-Min-Bars von `IG:DAX` gegen `XETR:DAX` (echter Orderbuch-Umsatz) für 10 überlappende Zeitstempel verglichen. Absolute Größenordnung nicht vergleichbar (Faktor ~3.000-6.000x, CFD-Ticks vs. echte Stückzahlen), aber Rangfolge/Ausschläge korrelieren moderat (Spearman ≈0,6), größter Volumen-Spike fällt bei beiden exakt auf denselben Bar. Bewertung: `IG:DAX`-Volumen ist als **relatives** Maß (Bar X volumenstark vs. Durchschnitt) plausibel und kein Fake-/Konstantwert — als absoluter Vergleichswert zum echten Xetra-Umsatz aber nicht geeignet. Reicht für technische Analyse (Volumen-Indikatoren, Bestätigung von Ausbrüchen etc.).

**Technisch/strukturell:**
4. ✅ **ERLEDIGT 06.08.2026:** `scripts/trade_db_dax.cjs` angelegt (1:1-Kopie von `trade_db.cjs`, Schema identisch, eigene Indizes `idx_trades_dax_*`). DB-Pfad `scripts/trades_dax.db`, override per `TRADE_DB_DAX_PATH` (bewusst eigener Env-Var-Name statt `TRADE_DB_PATH`, um Kollision zu vermeiden falls beide Module im selben Prozess geladen werden). Live getestet (Insert+Read via Node, danach Test-DB wieder geloescht). `scripts/trades_dax.db` zu `.gitignore` ergaenzt (analog `trades.db`). **Noch offen (nicht Teil von Punkt 4, aber laut Architektur-Entscheidung noetig vor dem ersten echten Eintrag):** `add_trade_dax.cjs`/`trade_stats_dax.cjs`-Pendants — `migrate_trade_log_dax.cjs` entfaellt, da es fuer DAX keine historischen Markdown-Trades zu migrieren gibt.
5. ✅ **ERLEDIGT 06.08.2026:** `memory/dax_trades/trade_log_dax.md` angelegt (identische Tabellenstruktur wie `trades/trade_log.md`, noch leer/bereit für ersten Eintrag). `cooldown_check.cjs` braucht keine Code-Änderung — liest bereits `TRADE_LOG_PATH` als Env-Var. Live getestet: `TRADE_LOG_PATH` auf die neue Datei gesetzt → korrekt "Keine Trades im Log gefunden — kein Cooldown aktiv." DAX-Aufruf-Befehl (Bash): `TRADE_LOG_PATH='C:\Users\umnus\.claude\projects\C--Users-umnus-tradingview-mcp\memory\dax_trades\trade_log_dax.md' node scripts/cooldown_check.cjs` — PowerShell: `$env:TRADE_LOG_PATH = 'C:\Users\umnus\.claude\projects\C--Users-umnus-tradingview-mcp\memory\dax_trades\trade_log_dax.md'; node scripts/cooldown_check.cjs`
6. ✅ **ERLEDIGT 06.08.2026:** `project_risikomanagement_dax.md` aufgesetzt — eigenständiges Dokument (kein Anhang ans NAS100-Dokument), enthält: Budget-Status (offen, verschoben auf Phase-4-Abschluss), eigene Phase-1-Logik (von vorne, unabhängige Zählung), Chartsymbol `IG:DAX`, Handelsfenster 10-15 Uhr, Overlap-Regel, Korrelationsregel (Referenz [[feedback_instrumenten_fokus]]), gemeinsames Trades/Tag-Limit, Cooldown-Mechanik-Verweis auf `trade_log_dax.md`. Abschnitt "Noch offen" listet unverifizierte NAS100-Übernahmen (ATR/Chop-Schwellen, VIX vs. VDAX-NEW als Volatilitäts-Proxy).
7. ✅ **ERLEDIGT 06.08.2026 (über den ursprünglichen Plan hinaus):** Statt nur FRED-Serien zu ergänzen, hat Levi live die echte EZB-Primärquelle gefunden (`data-api.ecb.europa.eu`, ECB Data Portal REST-API, PDF-Doku von Levi bereitgestellt und geprüft). `scripts/eu_snapshot_dax.py` angelegt, Mix aus zwei Quellen: **EZB direkt** für Hauptrefinanzierungssatz + Einlagesatz (kein API-Key, `/service/data/FM/D.U2.EUR.4F.KR.{MRR_FR|DFR}.LEV?format=jsondata&lastNObservations=1`, live 1 Tag aktueller als die FRED-Spiegelserie) — löst den in der Fable-Review vom 05.08. genannten Sekundärquellen-Vorbehalt auf, früher als geplant. **FRED** bleibt für HICP (Euroraum+Deutschland), deutsches Real-BIP, deutsche Arbeitslosenquote, deutsche 10J-Bundrendite (alle live aktuell getestet, Stand Mai/Juni 2026). Ausprobierte ECB-HICP-Serie (`ICP/M.U2.N.000000.4.ANR`) lieferte nur Stand Dez. 2025 (falsche Dimensions-Kombination, nicht weiter verfolgt — Zeitaufwand vs. Nutzen, FRED lief hier bereits sauber). Voller Testlauf erfolgreich, alle 7 Werte aktuell.
8. ✅ **ERLEDIGT 06.08.2026, final 7 Accounts nach zwei Levi-Korrekturrunden:** `scripts/x_watchlist_dax.json` angelegt, alle live über X-API verifiziert (Follower/Aktivität geprüft, keine toten Accounts). `@handelsblatt` (Breaking DE — ersetzt den zunächst gewählten Unteraccount `@hb_finanzen`, verifiziert, 472K), `@Schuldensuehner` (Sentiment/Makro, Holger Zschäpitz/Welt, 380K), `@ecb` (Institutionell — offizieller EZB-Account, verifiziert, 823K, ergänzt `eu_snapshot_dax.py`), `@zerohedge` (Risk-off, wiederverwendet aus NAS100-Watchlist), `@DeItaone` (neue Kategorie "breaking_global" — *Walter Bloomberg, ebenfalls aus NAS100-Watchlist wiederverwendet, 1,8M, globaler Markt-Ton), `@CWRoehl` + `@mario_lochner` (Kategorie "market_commentary" — Levi-Ergänzungen, Christian W. Röhl/Chief Economist Scalable Capital DACH 71,7K + Mario Lochner/Finanzbildung 41,4K). Verworfen: `@dpa_wirtschaft` (0 Tweets trotz Namensmatch, toter Account).
9. ✅ **ERLEDIGT 06.08.2026:** `feedback_session_update_dax.md` geschrieben, Trigger "start update dich #1", 6-Schritt-Ablauf 1:1 strukturell von NAS100 übernommen (Mechanik), aber mit allen heute gebauten DAX-Bausteinen befüllt: `x_watchlist_dax.json` (7 Accounts) + `x_last_fetch_dax.json` (neu angelegt, Timestamp geseedet), `eu_snapshot_dax.py`, ZEW/Ifo/Industrieproduktion als unverifizierte Kalender-Platzhalter markiert, Intermarket-Scan mit VDAX-NEW/EUR-USD/Bund-Rendite/EuroStoxx50/CAC40/FTSE100 (3 neue Symbole `TVC:DE10Y`, `TVC:CAC40`, `FTSE:UKX` live verifiziert), Cheat-Sheet explizit als "Entwurf, unverifiziert" gekennzeichnet (analog zur Architektur-Entscheidung — Mechanik ja, Schwellen nein). `x_last_fetch_dax.json` zu `.gitignore` ergänzt.
10. ✅ **ERLEDIGT 07.08.2026:** DAX-Chart-Layout eingerichtet — `IG:DAX`, Single-Pane (kein 2v-Split nötig, natives Volumen im Hauptsymbol), 5min, alle 6 Indikatoren (EMA50, Bollinger Bands, RSI, MACD, ATR, Volume). Als eigenes benanntes Layout "DAX" gespeichert, per UI-Klick verifiziert. **Wichtiger Nebenbefund:** Beim Aufbau wäre das live NAS100+QQQ-Layout beinahe versehentlich überschrieben worden (Layout-Tools verhielten sich unzuverlässig) — Vorfall erkannt und behoben, beide Layouts jetzt sauber getrennt gespeichert und verifiziert. Details/Lehren siehe [[feedback_layout_tools_unzuverlaessig]]. Zusätzlicher Mehrwert: Das bisher namenlose NAS100-Setup ("Nicht benannt") ist jetzt ebenfalls explizit als "NAS100" gespeichert (vorher nur ungesicherter Sitzungszustand).
11. ✅ **ERLEDIGT 07.08.2026** (vor Punkt 10 abgeschlossen, siehe Fable-Sequenzplan): [[feedback_live_trading_dax]] geschrieben (kein Volltext, Verweis auf Mechanik + explizite DAX-Abweichungen + unverifizierte Schwellen, analog Stil zu [[project_risikomanagement_dax]]/[[feedback_session_update_dax]]). Wichtigste Entscheidung: kein Dual-Gate-Ersatz, stattdessen 2-Kerzen-Bestätigungsstandard + weicher EuroStoxx50/CAC40-Sanity-Check (Fable-Empfehlung, Levi-bestätigt).
12. `feedback_session_update.md` (NAS100) um "#2"-Kennzeichnung ergänzen für konsistente Trigger-Benennung

**Vor dem ersten echten DAX-Trade mit Kapital — BRAUCHT LIVE-MARKTZEIT, nicht schreibtisch-erledigbar (bestätigt 07.08.2026):**
13. Mehrere neue Paper-Beobachtungssessions im DAX-Fenster (Fortsetzung von [[dax_beobachtung/uebersicht]], ruht seit 10.07.2026) — Schwellen gegen echtes Marktverhalten kalibrieren. Braucht echte laufende DAX-Handelszeit (10-15 Uhr DE), kann nicht vorbereitend erledigt werden.
14. Overlap-Szenario einmal bewusst live beobachten (DAX-Position noch offen bei 15 Uhr) — prüfen, wie gut "erst schließen, dann Nasdaq" in der Praxis funktioniert. Braucht eine echte offene DAX-Position im Übergang zu 15 Uhr, ebenfalls nicht vorab simulierbar.

**Timing-Entscheidung 07.08.2026 (präzisiert/verbindlich gemacht — löst die vagere "läuft parallel"-Formulierung aus der Timing-Klarstellung vom 05.08. unten ab):** Levi geht Punkt 13+14 bewusst erst an, **sobald NAS100 final Phase 4 abgeschlossen ist** — nicht opportunistisch währenddessen. Grund nicht explizit genannt, aber konsistent mit dem bisherigen Muster [[feedback_dont_change_running_system]]/[[feedback_instrumenten_fokus]]: voller Fokus auf NAS100 bis Phase 4 sauber durch ist, DAX-Paper-Kalibrierung erst danach als eigener, bewusster nächster Schritt — kein Nebenbei-Betrieb, der den NAS100-Fokus verwässern könnte.

**Status:** Punkt 1+2+4-9 erledigt (06.08.2026), Punkt 10+11+12 erledigt (07.08.2026) — Chartsymbol `IG:DAX`, `trade_db_dax.cjs` + `trade_log_dax.md` + Cooldown-Check (`cooldown_check_dax.cjs`) + `add_trade_dax.cjs`/`trade_stats_dax.cjs` + `project_risikomanagement_dax.md` + `eu_snapshot_dax.py` + `x_watchlist_dax.json` + `feedback_session_update_dax.md` + `feedback_live_trading_dax.md` + gespeichertes DAX-Chart-Layout + Trigger-Konsistenz "#1"/"#2" stehen. `size.cjs` zusätzlich abgesichert (Pflichtparameter `--kapital`, siehe [[project_regelwerk_audit_2026-08-07]]). Punkt 3 (Startbudget) bewusst auf Phase-4-Abschluss verschoben. **Einziger verbleibender Vorbereitungs-Rest: Punkt 13+14, beide gebunden an echte DAX-Live-Handelszeit — kein weiterer Schreibtisch-Schritt offen.**

**Nachtrag 07.08.2026 (nach erstem `start update dich dax`-Testlauf):** Levi hat zwei zusätzliche, über die ursprüngliche To-do-Liste hinausgehende Ergänzungen angefordert und mit Fable umgesetzt: (1) Intermarket-Tabelle von 5 auf 30 Symbole erweitert (siehe [[feedback_session_update_dax]] Schritt 5, alle live verifiziert), (2) automatische Pivot-Berechnung (Schritt 6a) + eine 3-Ebenen-Bias-Synthese-Methodik (Schritt 6b) als struktureller Ersatz für das fehlende Dual-Gate ergänzt. Beides noch am selben Tag live getestet (Pivots korrekt gezeichnet, Bias-Synthese lieferte ein plausibles NEUTRAL-Ergebnis trotz bullischem Intermarket-Quorum wegen MACD-H-Widerspruch — Mechanismus verhält sich wie beabsichtigt). Schwellen in Schritt 6b (≥3/5 etc.) sind wie der Rest des DAX-Regelwerks explizit unverifiziert, Review nach 5-10 Anwendungsfällen vorgesehen (siehe [[feedback_session_update_dax]]).

## Fable-Trennungs-Audit (07.08.2026) — auf Levis expliziten Wunsch, echtes Code-Lesen, kein Gedächtnis-Review

Levi: "Was mir wichtig ist, dass zwischen DAX Ordner und Nasdaq Ordner nichts vermischt wird — ich möchte das auf gar keinen Fall." Ergebnis: DB-/Ordner-Trennung ist sauber und sogar solider als erwartet (`trade_db.cjs`/`trade_db_dax.cjs` sind physisch getrennte SQLite-Dateien, `trades/` vs. `dax_trades/` ohne Überschneidung). Drei konkrete, aktuell noch "kalte" Lücken (kein akutes Risiko, da DAX noch ohne echtes Kapital), aber verbindlich vor dem ersten echten DAX-Trade zu schließen:

1. **`cooldown_check.cjs` hat kein DAX-Pendant, verlässt sich auf manuelles Env-Var-Setzen** (`TRADE_LOG_PATH`) bei jedem Aufruf — genau das fehleranfällige Muster, das schon beim Cooldown-Bruch bei Trade #12 eine Rolle spielte. Fix: `cooldown_check_dax.cjs` nach Vorbild von `trade_db_dax.cjs` mit eigenem Default-Pfad anlegen.
2. **`add_trade.cjs`/`trade_stats.cjs` haben keine DAX-Pendants** (bereits als "noch offen" in Punkt 4 oben vermerkt, hier nochmal mit Dringlichkeit bestätigt) — ein versehentlicher Aufruf für einen DAX-Trade würde in der NAS100-DB landen. Fix: 1:1-Kopien mit Import auf `trade_db_dax.cjs`.
3. **NEUER FUND — `size.cjs` hat einen ungesicherten NAS100-Kapital-Fallback:** `--kapital` ist optional mit Default 15.000€ (NAS100), `PHASE_DEFAULTS` sind auf NAS100 kalibriert. Kein `size_dax.cjs`, `project_risikomanagement_dax.md` erwähnt `size.cjs` nirgends. Ein DAX-Aufruf ohne explizites `--kapital` würde gegen die falschen NAS100-Zahlen rechnen — genau das befürchtete "in einen Topf werfen". Fix: `--kapital` zu Pflichtparameter machen (kein stiller Fallback) ODER eigenes `size_dax.cjs` sobald DAX-Budget (Punkt 3) feststeht.

Zusätzlich bestätigt: der bekannte [[feedback_pane_sync_bug]] bekommt durch den DAX-Parallelbetrieb (zwei Symbol-Sets im selben Fenster) mehr Gewicht als bisher, ist aber nicht DAX-spezifisch neu.

**Fazit:** Infrastruktur-Aufbau muss nicht gestoppt werden, aber die drei Skript-Lücken oben sind vor Punkt 3 (Startbudget) bzw. spätestens vor dem ersten echten DAX-Trade mit Kapital zu schließen — nicht sofort dringend, aber verbindlich vor dem Kapital-Schritt.
