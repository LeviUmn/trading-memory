---
name: project_testtag_analyse_2026-09-11
description: "Opus-Analyse Testtag 11.09.2026 (fiktiv, 460-Pkt-Trendtag): 51 Voll-Checks (nicht 52, VC#28 fehlt), 0 Trades, 18 Gate-PASS, 12 Fiktiv-Zeilen = 10 Setup-Momente, 6 PASS mit Q-UNBEKANNT undokumentiert. Zentraler Befund: Q2-Formel (Impuls-Reifegrad = volle Impuls-Amplitude / Live-ATR) war ab Loop-Sekunde 1 strukturell unerfuellbar (Min 9,1x, Median 14,2x, Max 16,5x bei Schwelle 1,5x) und bestraft fallende ATR als Ueberreife; Q2 allein blockte alle 10 Momente. 1 Prozessfehler (VC#30: add_skipped_setup.cjs schrieb in trades.db, selbst korrigiert, forensisch nur ueber sqlite_sequence=8 belegt), 2 widerlegte Faktenbehauptungen im Sonnet-Abschluss nach Kontextkompression (Screenshots: 6 bis 16:36 statt keiner; UNLOESBAR ab 15:47 statt 19:20). Status EINGESCHRAENKT. 12 Vorschlaege V1-V12; Fable hat V2/V3/V4/V9/V11/V12 umgesetzt, Opus-Gegencheck (Autor≠Pruefer) fand 1 Blocker (V3 = versteckte Regelaenderung, zurueckgebaut auf Begruendungspflicht --grund-chasing + Info-Kennzahl) und 2 Nachbesserungen (V4 Guard blind bis zum ersten Fiktiv-Eintrag -> Testtag-Modus im Zustandsspeicher ab VC#1; V2 Vollrewrite nicht atomisch + wiederholungen/ablehnungsgrund unsichtbar), alle behoben (Tests 57/57). Opus-Gegencheck Runde 2: kein Blocker, 2 Nachbesserungen umgesetzt (--testtag als Pflichtparameter von loop_prompt.cjs in jeden vollcheck-Aufruf injiziert statt handgepflegter Prompt-Text; add_skipped_setup.cjs trennt 'fiktiv belegt' von 'Modus nicht deklariert' in Meldung + Schreib-Log), Tests 60/60, Diff 10 Dateien ungepusht. V1/V10 als Entscheidungsvorlage, V5-V8 offen."
metadata:
  node_type: memory
  type: project
  status: analyse gespeichert, app-anpassungen umgesetzt + opus-gegencheck runde 1+2 behoben (ungepusht, levi committet), regelfragen offen
  originSessionId: session_01QEYrdXFdPSQJXVMQJHSUB4
  modified: 2026-09-11T21:57:18.411Z
---

## Kurzfazit (Opus, unabhängige Prüfung ausschließlich aus den Rohdateien in `scripts/`)

**Mechanisch regelkonform, fachlich mit drei Einschränkungen.** Bei 18 mechanischen PASS-Fällen und 4,3 h Loop wurde kein einziges Mal übersteuert — kein Trade gegen ein rotes Gate, kein Regelbruch. Nach 08./09.09. („Regelwerk besser, Disziplin nicht") ein echter Fortschritt, auch wenn er dadurch entstand, dass die blockierende Regel gar keine Wahl ließ. **Kernaussage: Der Ausführungsteil war sauber; der Engpass sitzt in der Regel (Q2).** Was wie 10 diszipliniert abgelehnte Setups aussieht, ist eine Ablehnung, zehnmal wiederholt, aus einem Grund, der sich in vier Stunden nicht ändern konnte.

## Zahlenbilanz (korrigiert)

- **Voll-Checks: 51, nicht 52.** `vollcheck_state.json`/`oneh_shadow_log.jsonl`: Nummern 1–27 und 29–52, **VC#28 fehlt** (Lücke 15:54→16:00 DE). Sonnets „52" war die höchste Nummer, nicht die Anzahl. Erster 15:40, letzter 19:55 DE (nicht 20:00); Kadenz exakt 5,0 Min. Vier von fünf Ticks mit >1 Min Drift fallen auf die Register-Touches — der 60-Min-Refresh kostet reproduzierbar 1–2 Min.
- **Trades/DB: 0/0**, `trades` max id 43 (21.08.), `skipped_setups` 6 Zeilen alle 10.09. **`sqlite_sequence.seq = 8` bei max id 7** = forensischer Beleg für den VC#30-Fehlschreibzugriff (ID 8 vergeben + gelöscht).
- **Fiktiv-Log: 12 Zeilen = 10 Setup-Momente** (VC#33/35–43; zwei byte-identische Doppelpaare 18:21/18:22 und 19:06/19:06, einziger Unterschied `chasing`). VC#34 fehlt.
- **Gate-Log: 49 Live-Läufe — 18 PASS, 18 FAIL, 11 Abbruch, 2 UNKNOWN.** Von 18 PASS: 12 Q-ROT (auto-erfasst), **6 Q-UNBEKANNT nirgends dokumentiert** (16:19, 16:19, 16:37, 16:42, 18:06, 18:16 DE). Sonnets „alle 12 = alle PASS" ist falsch.
- **FAIL: 16× rrGate, 2× tpRealismGate** — RR-Gate war mit 89 % der dominante Blocker, „RR-Gate, TP-Realismus Zone 3" suggeriert falschen Gleichrang.
- **SL-Vorprüfungen: 101 Läufe / 51 VC (Faktor 2,0), 0× UNTAUGLICH, 35× UNLÖSBAR ab 15:47 DE** (nicht „ab ca. 19:20") — Dauerzustand, Ursache laut Skript „Entry/SL-Geometrie, nicht TP-Wahl".
- **1H-Schatten** lückenlos, Bias durchgehend long, blockade_3v4 durchgehend false. **Register: 5 Touches, 4× reiner Zeitstempel-Refresh**, ein einziger inhaltlicher Eintrag (VC#9, Rundzahl 29550); zwei Fib-Extensions trugen 51 VC lang den Vermerk PROVISORISCH; Session-Hoch 29480,10 (Register) vs. Impuls-Extrem 29469,05 (State) = zwei Wahrheiten.

## Zentraler Befund: Q2 war mathematisch unerfüllbar (Kalibrierungsproblem, kein Ausführungsfehler)

Formel rekonstruiert: **Reifegrad = impuls_pkt / ATR** (VC#8: 429,75/47,0 = 9,14 ✓ … VC#43: 450,35/27,7 = 16,3 ✓). Zwei Defekte: (1) **entry-unabhängig** — VC#42/#43 (Entries 40 Pkt auseinander) bekämen identische Werte; der Unterschied stammt nur aus der ATR. (2) **Zähler konstant** (429,75 ab VC#1, 450,35 ab VC#23), **Nenner = Live-ATR fiel von 47,0 auf 20,6** → Reifegrad stieg von 9,1x auf 16,5x, *weil der Markt ruhiger wurde*. Messartefakt mit umgekehrtem Vorzeichen: je reifer die Konsolidierung, desto „überreifer" der Impuls, desto härter blockt Q2. Erfüllbar wäre Q2 erst bei ATR ≥ 300 Pkt. Verteilung 17 Werte: Min 9,14x, Max 16,5x, Median ≈14,2x bei Schwelle 1,5x — günstigster Wert = Sechsfaches der Schwelle. Der informative EMA50-Zweitanker lag in denselben Fällen bei 0,55/−0,02/0,21/−0,17/0,42/0,22/0,24/0,38/0,42/0,42 (VC#30/32: 1,53) — differenzierende Werte um die Schwelle. **Q2 war alleiniger Blocker**: 12/12 Fälle Q1 ✓ Q3 ✓ Q4 UNKNOWN Q2 ✗ → 2/4 ROT; mit Q2 ✓ wäre es 3/4 GELB = halbe Position = Freigabe gewesen.

Einordnung (Opus): am 09.09. waren 4 Q-ROT-Ablehnungen nachträglich korrekt (~2,9R vermieden), heute falsch — **ein Datenpunkt kippt keine Regel** → Schattenmodus (V1d) statt Sofortumstellung.

Weitere Befunde: **Q4 den ganzen Tag tot** (runway_ratio 12/12 null) und zirkulär mit dem Q2-Ergebnis begründet → effektiv 3-Faktoren-Score mit Deckel 3/4. **`--chasing` willkürlich** (17× yes / 3× no, zwei byte-identische Paare mit gegensätzlichem Flag im Abstand 8/13 s; „yes" neben `--entry-source "Retest INNERHALB des Fensters"`). **Long-Gate direkt nach selbst festgestelltem Kippsignal** (VC#33). **SL-Anker: 9 Wechsel in 4,3 h, 4 nach unten bei Long-Bias, keine einzige Begründung im Log** — nachvollziehbar dokumentiert, dem Kurs hinterherlaufend, nicht belegt regelkonform. **Schock-Ereignis (Pipeline ~18:55):** Ergebnis „keine Reaktion" aus Kursreihe belegt (+12,5 Pkt/15 Min bei ATR 28), aber **kein VIX-Wert irgendwo persistiert** — Methode zirkulär. **Letzte 34 Min blind:** letzter Gate-Aufruf 19:21, danach 8 VC ohne Gate, genau als der Kurs über TP1 von vier abgelehnten Setups stieg (29450,85 um 19:25).

## Prozessfehler

- **VC#30 — realer DB-Schreibzugriff am fiktiven Testtag** (mittel-hoch): irreversibler Zugriff auf die Produktions-Statistik, korrigiert durch manuellen DELETE (zweiter ungeschützter Direkteingriff). Root Cause strukturell: zwei namensähnliche Skripte (`add_skipped_setup.cjs` → trades.db, `skipped_fiktiv.cjs` → JSONL), das gefährlichere ohne `--dry-run`/Guard. Korrektur in keinem Log dokumentiert.
- **Screenshots:** Sonnets „kein Screenshot" ist falsch — 6 Aufnahmen bis 16:36 DE; danach **3 h 19 Min / 40 VC ohne einen einzigen**, genau die Phase mit allen 18 PASS. Sonnet hat nach Kontextkompression aus dem Sichtfenster ab VC#27 auf den ganzen Tag geschlossen, obwohl der Dateiordner verfügbar war — im „rein faktischen" Abschluss der schwerere Fehler. **Regel: nach jeder Kontextkompression Faktenabschluss ausschließlich aus den Dateien.**
- **Kein Protokolldokument** `memory/testtag/testtag_2026-09-11.md` (erster Testtag ohne) → `protokoll_bilanz.cjs`/`quote_check.cjs` nicht ausführbar, Chatverlauf komprimiert; alles außerhalb der Skript-Logs (Q1-Begründungen, Anker-Entscheidungen, VIX) unwiederbringlich weg.
- **`hypothetisches_ergebnis` 12/12 null** — `skipped_fiktiv.cjs --nachtrag` kein einziges Mal ausgeführt.

**Verpasste Chancen (Opus, 5-Min-Snapshots, nicht belastbar für TP1):** kein Setup hätte den SL getroffen (30–50 Pkt Abstand, belastbar); real wäre EIN Trade entstanden (VC#35, 18:31, Entry 29393,05, TP1 29450, RR 1,201), ~+0,6R netto bei Q-GELB/halber Position.

## Umsetzungsstand Fable (11.09.2026 abends, ungepusht, Tests `tests/trading_scripts.test.js` 57/57 grün nach Gegencheck; vor Gegencheck 56/56, davor 50/50)

| V | Status | Wo |
|---|---|---|
| **V4** Fiktiv-Guard | ✅ (nachgebessert) | `scripts/add_skipped_setup.cjs`: `--dry-run` (kein DB-Zugriff), Testtag-Guard, Append-only-Log `scripts/add_skipped_setup_log.jsonl` (ts, aktion, id, argv). **Nach Gegencheck:** Guard liest zuerst `scripts/vollcheck_state.json` (`testtag_modus` fiktiv/echt, gesetzt per `vollcheck.cjs --testtag fiktiv|echt` beim ersten Voll-Check, ohne Angabe aus dem State übernommen, Wechsel nur mit `--testtag-reset-grund`), dann das Fiktiv-Log; **fail-safe:** State für den Tag ohne Modus → ebenfalls Block; Modus `echt` gibt frei (Fiktiv-Einträge dann nur Hinweis, weil gate_check das Log auch an echten Tagen schreibt). Override `--echt-trotz-testtag` wird geloggt; gilt auch für `--repeat`. Testpfade `--fiktiv-log`, `--vollcheck-state`. `vollcheck.cjs` zeigt „Testtag-Modus FIKTIV/ECHT (CLI/State)" bzw. „NICHT gesetzt" in der Zustandsspeicher-Zeile. Echtdaten-Probe: Guard blockt heute über die 12 Fiktiv-Einträge (realer State von heute trägt noch keinen Modus). **Runde 2:** `--testtag` wird nicht mehr von Hand mitgegeben, sondern ist Pflichtparameter von `loop_prompt.cjs` und steht damit in jedem injizierten `vollcheck.cjs`-Aufruf (siehe Abschnitt unten). |
| **V2** Auto-Logger | ✅ (nachgebessert) | `gate_check.cjs` (Block „TODO 5 / V2"): greift bei Q-ROT **und** Q-UNBEKANNT, Feld `ablehnungsgrund`, Dedupe identisches (entry, sl, tp1, dir) innerhalb 5 Min → `wiederholungen`+1 statt neuer Zeile; im Eintrag `chasing`, `chasing_grund`, `chasing_abstand_atr` (informativ), `sl_anker`, `impuls_reifegrad_atr_emaanker`. **Nach Gegencheck:** Vollrewrite der JSONL atomisch (Temp-Datei + `renameSync`, auch in `skipped_fiktiv.cjs --nachtrag`); `skipped_fiktiv.cjs --list` zeigt je Zeile `Grund: Q-ROT/Q-UNBEKANNT/manuell` + `Wdh. N (zuletzt HH:MM DE)` und eine Summenzeile „Setup-Momente: N Zeile(n), Summe 1+wiederholungen = M Gate-Läufe | Ablehnungsgründe …"; `protokoll_bilanz.cjs` neue Zeile „Fiktiv-Log skipped_setups_fiktiv.jsonl (maschinell, V2)" + JSON-Feld `ankerOhne7b1.skippedFiktiv` + Abweichung, wenn Einträge ohne hypothetisches Ergebnis (nicht bei `--nur-zaehlung`); Alt-Einträge ohne Feld werden aus dem `quelle`-Text abgeleitet. |
| **V3** Chasing berechnet | ❌ **ZURÜCKGEBAUT** (Opus-Blocker) | `gate_check.cjs`: `--chasing yes\|no` ist wieder unbedingtes A3-Pflichtfeld (`MEASUREMENT_REQUIRED`), Status rein manuell nach Punkt 13. **Neu:** `--grund-chasing "<Begründung>"` ist Pflicht, sobald `--chasing` gesetzt ist (Hard-Exit 1 „CHASING OHNE BEGRUENDUNG"), Grund wird in der Chasing-Statuszeile ausgewiesen; ohne `--chasing` behält `--grund-chasing` die alte A3-Ausnahme-Bedeutung. Die Kennzahl \|Entry − SL-Anker\|/ATR bleibt als eigene Zeile „Chasing-Kennzahl (informativ, kein Sizing-Einfluss)" mit Vergleichsmarke 2x (Konstante `CHASING_ANKER_ATR_MULT`, unkalibriert) — beeinflusst weder Ampel noch Sizing noch Pflichtfelder. Fehlerhafter Beleg „0,65–1,3x" aus dem Kommentar entfernt, Konstante neutral als Beobachtungsgröße beschrieben. `vollcheck.cjs:153/728`: CHASING_KERZEN-Logik unverändert kerzenbasiert, 13.1-Text verweist jetzt auf `--chasing yes --grund-chasing`. |
| **V9** Anker-Wechsel-Pflichtgrund | ✅ | `gate_check.cjs` `slAnkerWechselGuard()`: Anker ≠ Anker der letzten `last_sl_vorpruefung.json` (gleicher DE-Tag, gleiche Richtung) → `--grund-sl-anker` Pflicht (Exit 1 ohne), Warnzeile bei Wechsel entgegen Trade-Richtung, Log `scripts/sl_anker_wechsel_log.jsonl`, Feld `anker_wechsel` in last_sl_vorpruefung.json. Gilt in `--sl-vorpruefung` und live; `--dry-run` nur Anzeige. `vollcheck.cjs` reicht `--grund-sl-anker` an den Unterprozess durch und zitiert die V9-Zeilen. Kein Ausweg für fehlendes `--sl-anker` (Auflage 3 bleibt). |
| **V12** VC-Lücke | ✅ | `vollcheck.cjs` (State-Block am Ende): `nr !== letzte_nr + 1` → Pflichtzeile `**VOLL-CHECK-LÜCKE (V12):** … Bilanz: N ausgeführt, höchste Nummer M, Lücke bei #…`; danach bis Tagesende Zeile `Voll-Check-Zählung (V12)`. |
| **V11** geprueft/updated | ✅ (Kern) | `register_constants.cjs` (+`registerFrischeStamp`, `registerInhaltWarnung` 180 Min, `provisorischeLevel/Warnung`), `register_touch.cjs` (schreibt `geprueft` immer, `updated` nur bei Inhaltsänderung; Log `geprueft_new`/`inhalt_geaendert`; `--provisorisch-bestaetigt`/`--provisorisch-loeschen <price>`), `register_check.cjs` (60/90 an `geprueft`, INHALT-WARNUNG + PROVISORISCH-Pflichtzeile in der zitierten Zeile), `gate_check.cjs` loadLevelRegister + printResult, `vollcheck.cjs` Touch-Beleg über `geprueft_new`. Echtdaten-Probe: „INHALT-WARNUNG 215 Min + PROVISORISCH 2 Level". **Offen aus V11:** Divergenz-Meldung Register-Session-Extrema vs. `vollcheck_state.impuls_extrem`. |
| **V1a–c** Q2-Kalibrierung, **V1d** Schattenmodus, **V10** Q4 | ⏸ Entscheidungsvorlage | Regelfragen (`feedback_chartanalyse.md`) — nicht implementiert, siehe [[project_q2_kalibrierung_entscheidungsvorlage_2026-09-11]]. V1d bewusst nicht vorab gebaut: das Schatten-Logging setzt bereits die Wahl des neuen Ankers voraus (Levis Entscheidung). |
| **V5** Protokoll aus Logs, **V6** Screenshot-Zwang, **V7** Schock-Log, **V8** Auto-Nachtrag | ⏸ offen | größere neue Features, Priorität lag auf den Bugfix-Punkten; nicht begonnen. V8 hängt zusätzlich an einer Kursreihen-Quelle im Tagesabschluss. |

Testanpassungen (Stand nach Gegencheck): `GATE_BASE` setzt wieder `--chasing no` + `--grund-chasing` per Default (`o.chasing === null` lässt beide weg, `o.chasingGrund === null` nur den Grund); P6-Test prüft fehlendes `--chasing` = Exit 1, `--chasing` ohne Grund = Exit 1, Kennzahl-Zeile 43,92 Pkt = 1,21x, A3-Ausnahme ohne `--chasing`; V3-Test prüft, dass 3,03x ATR weder Status noch Sizing ändert; V2-Test prüft `.tmp`-Reste, `--list`-Sichtbarkeit und `protokoll_bilanz` JSON/Text; V4-Test prüft die 16-Minuten-Lücke (State fiktiv + leeres Fiktiv-Log = Block), State ohne Modus = Block, Modus echt = frei; neuer V4b-Test für `vollcheck.cjs --testtag`. TODO-6-Test läuft gegen leere Quellen (`--fiktiv-log`, `--vollcheck-state`). describe-Block „Opus-Analyse Testtag 11.09.2026" jetzt 7 Tests.

## Opus-Gegencheck der Fable-Umsetzung (11.09.2026 abends, Autor≠Prüfer) — 1 Blocker, 2 Nachbesserungen, alle behoben

- **Blocker V3 (versteckte Regeländerung, kein Bugfix):** (a) Mit `--sl-auto` (SL = Anker ± 0,5×ATR) ist \|Entry − Anker\|/ATR algebraisch = SL-Distanz/ATR − 0,5 — eine Stop-Breiten-Kennzahl, kein Momentum-Maß; Punkt 13 definiert Chasing als Kerzenfolge. (b) Zwei widersprüchliche Definitionen im System (`vollcheck.cjs` kerzenbasiert vs. `gate_check.cjs` abstandsbasiert). (c) Kommentar-Beleg „0,65–1,3x" falsch: real 0,48–1,91x; 5 der 12 Q-ROT-Fälle hatten manuell `yes` → V3 hätte volle Position gerechnet (risikoerhöhend); über 44 Live-Aufrufe 15× „yes" (bis 3,15x), 7× gegen manuelles `no`, darunter das TAUGLICH-Setup VC#54 vom 10.09. (2,17x). (d) `--chasing` aus `MEASUREMENT_REQUIRED` gestrichen = A3-Pflichtfeld ersatzlos weg. **Lösung (Opus, von Levi bestätigt):** Begründungspflicht allein heilt den belegten Befund (17× yes / 3× no bei byte-identischen Setups); Kennzahl nur Anzeige.
- **Nachbesserung V4:** Guard feuerte nur bei vorhandenen Fiktiv-Einträgen des Tages; erster Eintrag 18:21 DE, VC#30-Fehlschreib 18:05 — 16 Minuten blind. Befund bei der Umsetzung: es gab **keinen** expliziten Fiktiv-Marker im Projekt (`vollcheck_state.json` trägt nur `datum_de`/`erster_vollcheck`, an echten Tagen genauso); der Fiktiv-Log ist als Signal zusätzlich unscharf, weil gate_check ihn an echten Tagen auch schreibt. Deshalb neuer, minimaler Marker `testtag_modus` im Zustandsspeicher (ein Flag an VC#1), fail-safe bei fehlender Deklaration.
- **Nachbesserung V2:** Dedupe ersetzte `appendFileSync` durch ein Vollrewrite via `writeFileSync` (Abbruch = ganze Datei weg) → atomisch; `wiederholungen`/`ablehnungsgrund` las kein Konsument → in `--list` und Tagesbilanz sichtbar.
- V9/V11/V12 committierbar wie sie sind (Randfälle nur als Kommentar). Q2/Q4-Entscheidungsvorlage unangetastet (Schattenlogging läuft, Neubewertung in ~10 Testtagen). Levis Rahmenvorgabe für die Session: kein Aufblähen des Regelwerks — nur die Begründungspflicht und ein Marker-Flag kamen dazu, keine neue Schwelle.
- Kein Commit/Push durch Fable — Levi committet selbst.

## Opus-Gegencheck Runde 2 (11.09.2026 spät) — kein Blocker, 2 Nachbesserungen, beide umgesetzt (Tests 60/60)

Opus bestätigte die Korrekturen aus Runde 1 (57/57 grün, kein Blocker) und empfahl vor dem Commit zwei Nachbesserungen:

1. **Testtag-Flag gehört in `loop_prompt.cjs`, nicht in handgepflegten Cron-Prompt-Text** (wichtig). Fables Vorschlag „ab dem nächsten Testtag `--testtag fiktiv` in den Cron-Prompt aufnehmen" war die fehleranfällige Variante (einmal vergessen = Fail-Safe-Block den ganzen Tag). Umgesetzt in `scripts/loop_prompt.cjs`: `--testtag fiktiv|echt` ist **Pflichtparameter** (Exit 1 ohne/ungültig, gleiches Muster wie die Template-Prüfung; Ausnahme `--nur-templates` = reiner Quellen-Dump), wird analog zu `--state` direkt dahinter in Template A injiziert (Template trägt schon `--testtag` → nur ersetzt; Template ohne `--state` → beides eingefügt) und bekommt eine Erklärzeile im Block („ein fiktiver Testtag ist aus den Daten NICHT erkennbar … identischer Wert bei jedem Fire läuft stumm durch, selbstheilend, abweichender Wert verlangt `--testtag-reset-grund`"). Nebeneffekt = Vorteil: das Flag steht bei **jedem** der ~250 Ticks in der Kommandozeile, nicht nur bei VC#1. `vollcheck.cjs` verifiziert, **nicht geändert**: Zeile 364 kippt nur bei CLI ≠ State (dann `--testtag-reset-grund`), identische Werte laufen stumm durch — jetzt zusätzlich per Test abgesichert (V4b-Test, Fall „identischer CLI-Wert bei VC#2"). Loop-Start-Checkliste in [[feedback_live_trading]] Punkt 1/3 nachgezogen (Aufruf mit `--testtag`, Selbsttest prüft jetzt auch `--testtag`). `feedback_vollcheck_format.md` unangetastet.
2. **Meldungstext in `add_skipped_setup.cjs` präzisiert** (kosmetisch). Im Fail-Safe-Zweig (Modus nicht deklariert) druckte der Override „Echt-Eintrag an einem fiktiven Testtag" — an einem echten, nur undeklarierten Tag eine falsche Behauptung im Protokoll und Schreib-Log. Jetzt trägt `testtagBefund()` ein Feld `art` (`fiktiv` = State-Modus fiktiv oder Fiktiv-Log-Einträge; `undeklariert` = State ohne Modus); Guard-Exit-1-Text, Override-Vermerk (`overrideVermerk()`, insert UND `--repeat`) und Schreib-Log (`override_art`) unterscheiden Fall A „Echt-Eintrag an einem FIKTIVEN Testtag" von Fall B „Testtag-Modus NICHT deklariert (weder `--testtag` im Zustandsspeicher noch Fiktiv-Log-Eintrag für den Tag): Override erzwungen, im Protokoll begründen, ob dies ein echter oder fiktiver Tag war".

Tests: neuer describe-Block „Opus-Gegencheck Runde 2" mit 3 Tests (Pflichtparameter/Exit 1/`--nur-templates`; Injektion fiktiv/echt/abweichende State-Datei/Template-Varianten mit eigener Sandbox-Formatdatei; Fall A vs. B in Konsole + Log inkl. `--repeat` und Modus echt), plus Ergänzungen im V4- (Fall-A-Text, `override_art`) und V4b-Test (Idempotenz). Echtlauf gegen die reale `feedback_vollcheck_format.md`: Kommandozeile `node scripts/vollcheck.cjs --state scripts/vollcheck_state.json --testtag fiktiv --jetzt …` + Erklärzeile, ohne `--testtag` Exit 1. Gesamt-Diff (ungepusht, Levi committet selbst): 10 Dateien — `add_skipped_setup.cjs`, `gate_check.cjs`, `loop_prompt.cjs`, `protokoll_bilanz.cjs`, `register_check.cjs`, `register_constants.cjs`, `register_touch.cjs`, `skipped_fiktiv.cjs`, `vollcheck.cjs`, `tests/trading_scripts.test.js`.

## Status

**EINGESCHRÄNKT** (Opus) — fehlende Protokolldatei, fehlende Screenshots ab 16:36 DE, zwei widerlegte Faktenbehauptungen im Tagesabschluss. Auflagen für den nächsten Testtag (aus den Befunden abgeleitet, keine neue Handelsregel): Protokolldatei ist Pflicht (ohne sie ist der Tag nicht auditierbar), Faktenabschluss nur aus Dateien, `skipped_fiktiv.cjs --nachtrag` im Tagesabschluss, Q2-Entscheidung von Levi vor dem nächsten Trendtag.

Vorgänger: [[project_testtag_bilanz_2026-09-10]], [[project_testtag_analyse_2026-09-09]]. Rohdaten: `scripts/vollcheck_state.json`, `gate_check_log.jsonl`, `skipped_setups_fiktiv.jsonl`, `oneh_shadow_log.jsonl`, `register_touch_log.jsonl`, `trigger_kandidaten_log.jsonl`, `level_register.json`, `trades.db`.
