---
name: feedback-vollcheck-format
description: "Voll-Check-Ausgabeformat: Fließtext mit ✓/✗ hinter jedem geprüften Punkt, keine Tabelle. Seit 31.08.2026: Voll-Check-Nr. mit ausgeschriebener Uhrzeit-Rechnung in der Kopfzeile, 8d-Zeile mit Einzelausweis aller drei Kriterien, QQQ-VWAP-Vergleich fest an die AVWAP-Session-Instanz gebunden (Erkennung in data_get_study_values über die Band-1-Breite, nie über Listenposition) Template A seit 11.09.2026: --cluster-level und --sl-anker Pflicht ohne Default und ohne --grund-Ausweg sobald die SL-Anker-Vorprüfung fällig ist (TODO 3, Opus-Gegencheck Auflage 3); SL-Kandidat regelkonform per gate_check --sl-auto (MAX Anker-Puffer/Cluster-Kante/8c-Floor); --trend-effizienz optional (Chop-Verdacht nur Ausweis, unkalibriert — Rückbau im Opus-Gegencheck 11.09.)."
metadata:
  node_type: memory
  type: feedback
  originSessionId: 7128a95a-a9a6-42e6-8a59-8f22dec5c654
  modified: 2026-09-15T10:00:32.253Z
---

Im Live-Loop-Voll-Check (siehe [[feedback_loop_ablauf_uebersicht]] Szenario 1, jede 5. Minute) das Ergebnis als **Fließtext mit ✓/✗ direkt hinter jedem geprüften Punkt** ausgeben — keine Markdown-Tabelle.

**Why:** Am 27.07.2026 (Live-Trading-Tag, Phase-3-Start) auf Tabellen-Format umgestiegen (eine Zeile pro Ebene: 1H/15min/5min/QQQ/Regime). User hat das als Abweichung vom bewährten Design erkannt: die Tabelle zeigt zwar Werte, aber nicht explizit, OB und WAS an Regeln/Kriterien tatsächlich durchgecheckt wurde — genau das ist der Zweck der bestehenden Offenlegungspflicht ([[feedback_loop_ablauf_uebersicht]] Szenario 1, Punkt 9.7: "im Output kurz nennen, was geprüft wurde und was ausgelassen wurde"). Das ursprüngliche Format nutzte dafür ✓/✗ direkt hinter jeder Teilprüfung (Muster aus [[feedback_live_trading]] Punkt 7a: "Indikatoren jetzt erfüllt ✓" / "nicht mehr erfüllt ✗"), damit User und Claude selbst auf einen Blick sehen, welche Kriterien tatsächlich bestätigt/nicht bestätigt/ausgelassen wurden — eine Tabelle mit reinen Zahlenwerten verwischt das.

**How to apply:** Jeder Voll-Check-Output folgt diesem Muster — jede Ebene startet in eigener Zeile mit **fettem Label**, Inhalt bleibt Fließtext-Satz mit ✓/✗ (kein Tabellen-Raster, aber klar getrennt statt einem dichten Absatzblock, siehe Korrektur 28.07.2026):

```
Voll-Check <HH:MM:SS, echte Systemzeit per `date`> (Nr. <N> = (<Minuten seit erstem Voll-Check>)/5+1) — <1-Satz-Einordnung>

**1H NAS100:** RSI <x> ✓/✗ <bearish/bullish bestätigt>, MACD-H <x> ✓/✗, <EMA50/BB-Status> ✓/✗. Struktur: <HH-HL/LH-LL-Kurzfazit>.

**15min NAS100:** RSI <x> ✓/✗, MACD-H <x> ✓/✗.

**5min NAS100:** RSI <x>, MACD-H <x> (Trigger-Ebene, meist noch → warten).

**QQQ 15min:** <Kurs> unter/über EMA50 ✓/✗, unter/über VWAP (Session-Instanz) ✓/✗, Volumen <erhöht/normal> ✓/✗.

**MTF-Frische:** Referenzkerzen-Alter ab Kerzen-SCHLUSS: 1H <x> Min (≤122) | 15min <x> Min (≤32) | 5min <x> Min (≤12) | Spreizung <x> Min (älteste Ebene: <Ebene>).

**Regime-Gate (8d):** Schock-Tag ✓/✗ (<Kriterien-Zahl>/3) [Range <X> Pkt / ATR-D <Y> = <Z>× (Stand HH:MM) ✗/✓ | Makro-Häufung ✗/✓ (<m> qualifizierende Events: <Name Ist/Konsens, …> oder "keine") | VIX-Range <W>% & VIX <akt> vs. VT-Schluss <VS> → ✗/✓ oder "nicht gemessen"] [K3-Schatten (GEMESSEN, KEIN GATE): VIX≥20? ✗/✓ | Δ VT-Schluss <x>% ≥+10%? ✗/✓ → K3-neu ✗/✓] | Regime: <Trend/Chop>.

**ADX(14, NAS100):** <Wert> (GEMESSEN, KEIN GATE).

**Zählstände:** 8a5-Zyklus <n> (Level: <X>) | Kerzen seit Gegenseite NAS100(5min) <n> / QQQ(15min) <n> — fortlaufend, aus Vorcheck +Delta.

**Chartmuster/Fibonacci:** <geprüft, Ergebnis> ODER <ausgelassen, Grund — "ausgelassen" nur, wenn KEIN Impuls ≥2×ATR mit Korrektur vorliegt; sonst Extension 1.272/1.618 = <X>/<Y>, ins Register eingetragen ✓>.

**Chasing-Status (13):** <n> gerichtete Kerzen seit letzter Konsolidierung | Kriterien erfüllt ✓/✗ | Voll-Checks ohne Punkt-11-Signal: <k>/2 | 13.1-Konsequenz: <keine / beobachten / 50 %-Einstieg AKTIV vorschlagen>. *(seit 15.09.2026, Option D + b1: k zählt nur bei vollständigem Dual-Gate — bei Bruch "keine — Dual-Gate x/2 unvollständig, k-Zähler zurückgesetzt"; bei Q-ROT aus dem letzten Gate-Lauf "beobachten — Q-ROT geht vor (Option D)")*

[**13.1 × Q-Score-Kollision:** Chasing-Kriterien seit <k>/2 erfüllt → 50% Einstieg | Q-Score <n>/4 ROT (Q<x>/Q<y> ✗) → Auslassen empfohlen | Regel (Option D): <Q-ROT geht vor, 13.1 = beobachten | 13.1 greift regulär (ROT nur aus Q2)>] *(Pflicht-Kombizeile nur bei erkannter Kollision = 13.1-Kriterien mit BEKANNTEM k ≥ 2 UND Q-ROT/UNBEKANNT — `vollcheck.cjs` druckt sie bei k ≥ 2 (Kerzen + volles Dual-Gate) mit Q-ROT/UNBEKANNT aus dem letzten Gate-Lauf des Tages gleicher Richtung, `gate_check.cjs` bei `--chasing yes` + PASS + ROT/UNBEKANNT + übergebenem `--k-ohne-signal` ≥ 2. Fehlt k bei `gate_check.cjs` oder ist k < 2, druckt es stattdessen `13.1 × Q-Score-Kollision: NICHT bewertet — k nicht uebergeben bzw. k<2/2 (…); keine 13.1-Praezedenz angewandt, kein Schattenmessungs-Eintrag` (kein Solo-Default, kein Marker, Q-ROT dort kein Veto) plus Plausibilitäts-Warnung; Levi-Entscheidung 15.09.2026 + N1 des Opus-Gegenchecks 15.09.2026 (Option b verschärft), Regeltext [[feedback_live_trading]] 7b1a "Präzedenz gegenüber Punkt 13.1". Im Deckel-Fall nach PASS+UNBEKANNT verweist die Konsequenz auf "neuer Trigger" statt auf die Retest-Zeitbox, die nur nach PASS+ROT startet (N6).)*

[**Retest-Zeitbox:** VC+<1|2>/2 nach Gate-<FAIL|UNKNOWN|PASS+Q-ROT> (VC#<N>) — Bedingung <aus RETEST-ZEITBOX-Skriptzeile> <erfüllt/nicht erfüllt> → <Neu-Aufruf jetzt / warten / VERFALLEN> ODER (bei `--position offen`) `n.a. (Position offen — Zeitbox informativ)`.] *(nur in den zwei Voll-Checks nach einem gate_check.cjs-FAIL/UNKNOWN — seit 09.09.2026 Abend auch nach PASS mit Q-Score ROT [TODO 9]; die Pflichtprüfung/A3-Feld `--retest-bedingung` gilt dabei nur, solange keine Position offen ist [G5-Fix, Option (d), 09.09.2026] — der Grundtext behauptet nie retrospektiv "ausgelassen", da `gate_check.cjs` das zum Gate-Zeitpunkt strukturell nicht wissen kann; der VERFALLEN-Baustein wird von `vollcheck.cjs` beim ersten Voll-Check nach Ablauf der Wanduhr-Deadline erzwungen und genau einmal ausgewiesen, unabhängig von `--position`)*

**Tweet-Check:** <Raster-Slot HH:M0 fällig+durchgeführt ✓ (nichts Neues/kurzer News-Hinweis)> ODER <nicht fällig, nächster Raster-Slot HH:M0, letzter Fetch vor Xmin (x_fetch_stamp --check: JA/NEIN)> ODER <ÜBERFÄLLIG ✗ — Slot HH:M0 verpasst, jetzt nachgeholt>. (Fälligkeit = DOPPELBEDINGUNG, Stand 03.09.2026: (a) festes Kerzenraster, echte Minute % 10 == 0 [Kerzenraster-Fix 28.08.2026, siehe [[feedback_live_trading]] Punkt 9] UND (b) Minuten-Delta seit dem letzten ABRUF aus `x_last_fetch.json` (Feld `last_poll`, nicht der Tweet-Wasserstand `last_fetch` — F-B 14.09.2026) ≥10 — Delta und fällig-JA/NEIN liefert `node scripts/x_fetch_stamp.cjs --check` fertig gerechnet, Ergebnis gehört in die Zeile; siehe Abschnitt "Tweet-Fälligkeit rechnerisch" unten. Xmin = das vom --check gelieferte Minuten-Delta. Der Zeitstempel selbst wird NIE roh/UTC im Output gezeigt, siehe Korrektur unten.)

**Register-Check:** <erste Ausgabezeile von `node scripts/register_check.cjs` WÖRTLICH — `Register-Check: OK|WARNUNG|HART — Alter <x> Min … | updated … | <n> Level …`> | Exit-Code <0|1|2> | <bei 1/2: register_touch.cjs-Aufruf in diesem Voll-Check ausgeführt: `register_touch: updated … -> …`>

**1H-Schatten (P8-Messung, kein Gate):** Abstand Kurs↔1H-EMA50 <x> Pkt = <y>× ATR(5min) | 3/4-Blockade: <ja/nein> (ja = 5min+15min NAS100+QQQ richtungsgleich, nur 1H-Override dagegen; zählt erst ab ≥15 Min Dauer UND ≥1,5×ATR Bewegung in blockierter Richtung — E7) → `oneh_shadow_log.jsonl` ✓

[**SL-Anker-Vorprüfung (8c2):** <URTEIL-Zeile von `node scripts/gate_check.cjs --sl-vorpruefung --entry <Kurs> --sl <Struktur-Anker> --dir <long|short> --atr <ATR5min> [--tp1 <Kandidat>]` wörtlich → SL-ANKER TAUGLICH / UNTAUGLICH (konformer SL <x>, TP1-Fenster <…>)>] *(nur in Voll-Checks mit beobachtetem Setup — Dual-Gate ≥1 Bein oder aktive Levelsuche; E3, 07.09.2026)*

**Format:** Fließtext ✓ | Voll-Check #<N> vollständig (MTF ✓ | MTF-Frische ✓ | 11b ✓ | 8d ✓ | ADX ✓ | Zählstände ✓ | Chasing ✓ | Tweet-Check ✓/n.a. | Register-Check <OK|WARNUNG→touch|HART→neu> (Exit <0|1|2>) | 1H-Schatten ✓ | Screenshot ✓) [| Order-Sperre 15:00–15:30 aktiv] [| Halbierungsfenster 15:30–16:00 aktiv] [| Retest-Zeitbox VC+k/2] [| Terminalbedingung 21:45 geprüft ✓]

🟢/🔴/→warten <Fazit>
```

**VWAP-Instanz-Bindung in der QQQ-Zeile (ergänzt 31.08.2026, Opus-Live-Prüfung nach der Zwei-Instanzen-Anlage, Levi-Freigabe):** Seit 31.08.2026 laufen auf QQQ ZWEI identisch benannte `Anchored VWAP (Remote)`-Instanzen (Session-Instanz `in_2=1`, dauerhaft `in_0=0`; Ereignis-Instanz `in_2=1.5` — siehe [[feedback_chart_layout]], Abschnitt "Zwei AVWAP-Instanzen"). Der "unter/über VWAP"-Vergleich in der QQQ-Zeile oben meint IMMER und AUSSCHLIESSLICH die **Session-Instanz** — nie die Ereignis-Instanz, nie "die VWAP-Studie" pauschal. Praktische Zuordnung, weil `data_get_study_values` (die Datenquelle dieser Zeile) beide Einträge mit identischem Namen und OHNE Entity-ID/`in_2`-Feld liefert: den Eintrag mit dem **schmaleren Band 1** nehmen (Abstand Band-1-Ober-/Unterlinie zur AVWAP-Mittellinie — bei der Ereignis-Instanz ist dieser Abstand durch `in_2=1.5` genau 1,5× so groß wie bei der Session-Instanz; Band 2 taugt NICHT als Merkmal, Multiplikator 2 bei beiden). NIE über die Listenposition zuordnen — `data_get_study_values` und `chart_get_state` listen die beiden Instanzen in UMGEKEHRTER Reihenfolge (siehe Reihenfolge-Falle in [[feedback_chart_layout]]). **Why gerade jetzt:** Solange kein Trade offen ist, zeigen beide Instanzen denselben VWAP-Wert (beide `in_0=0`) und der Fehler bleibt unsichtbar. Sobald aber eine Position offen ist und die Ereignis-Instanz einen echten Anker trägt, divergieren die beiden Werte — genau in dem Moment, in dem die Pflichtzeile am wichtigsten ist, würde eine unspezifizierte "VWAP" mehrdeutig. Bei divergierenden Werten (offener Anker) im Zweifel zusätzlich per `data_get_indicator` + `in_2` auf den Entity-IDs gegenprüfen, statt zu raten.

Jedes ✓/✗ steht für eine ECHTE Einzelprüfung, nicht dekorativ — bei "ausgelassen" (z.B. kein Impuls für Fibonacci) explizit als "ausgelassen" kennzeichnen statt ✗ zu setzen (✗ = geprüft und nicht erfüllt, "ausgelassen" = gar nicht geprüft, das ist ein Unterschied).

**Korrigiert 28.07.2026 (User-Wunsch nach mehr Struktur, live während Trading-Session):** Der ursprüngliche dichte Absatzblock (alle Ebenen in einem durchgehenden Fließtext ohne Zeilenumbruch) wurde als schwer lesbar empfunden. Lösung: Zeilenumbruch pro Ebene mit fettem Label, Satzform pro Ebene bleibt erhalten — explizit KEINE Rückkehr zur Tabelle (die 27.07.-Korrektur bleibt gültig), sondern eine Zwischenstufe mit mehr visueller Trennung. Zwei Alternativen (Stichpunkte, unveränderter Block) wurden dem User zur Wahl vorgelegt, diese Variante wurde ausdrücklich gewählt.

**Tweet-Check- und Format-Zeile ergänzt 27.07.2026 (Fable-Tagesabschluss-Review, nach zwei Prozessfehlern desselben Tages — siehe [[feedback_live_trading]] Punkt 9 "Struktureller Anti-Drift-Fix" und [[feedback_prozessfehler_27_07_fuer_fable]]):** Beide Zeilen sind ab jetzt fester, nicht optionaler Bestandteil jedes Voll-Check-Outputs — unabhängig davon, ob der Tweet-Fetch diese Runde überhaupt fällig ist oder ob "alles wie gewohnt" aussieht. Root-Cause war, dass ein "stiller" Check (kein sichtbarer Output bei "nichts zu tun") nicht von einem schlicht vergessenen Check zu unterscheiden war — die beiden Zeilen hier machen den Zustand jeder Runde sichtbar und für den User gegenprüfbar, statt sich auf reines Erinnern über eine lange Tick-Folge zu verlassen. Fehlen sie im Output, gilt der Voll-Check als nicht durchgeführt.

**Zeitzonen-Fix in der Tweet-Check-Zeile (ergänzt 25.08.2026, Opus-Vorschlag 2 aus dem Testtag-Review, siehe [[project_testtag_analyse_2026-08-24]]):** Die Vorlage in Zeile 32 oben ist die korrigierte, ab jetzt geltende Fassung. ~~Die alte Fassung `<fällig+durchgeführt ✓ (nichts Neues/kurzer News-Hinweis)> ODER <noch nicht fällig, zuletzt vor Xmin> ODER <ÜBERFÄLLIG ✗ → jetzt nachgeholt>` nannte das Minuten-Delta nur im mittleren Zweig und sagte nichts zur Zeitzone des zugrundeliegenden Zeitstempels aus~~ — gilt nicht mehr. Ab jetzt zwei feste Pflichten für diese Zeile: (a) jeder aus `x_last_fetch.json` gelesene Fetch-Zeitstempel wird, falls überhaupt als Uhrzeit ausgegeben, explizit in Ortszeit umgerechnet und mit "(Ortszeit)" gekennzeichnet — nie die rohe UTC-Zeitangabe unkommentiert übernehmen; (b) die Zeile nennt in JEDEM der drei Zweige das reine Minuten-Delta seit dem letzten Fetch, nicht nur einen Uhrzeit-Vergleich.

**Why:** Am Testtag 24.08.2026 verglich Sonnet den UTC-Zeitstempel aus `x_last_fetch.json` direkt mit den in Ortszeit geführten Loop-Uhrzeiten und berechnete daraus fälschlich eine "3-Stunden-Tweet-Fetch-Lücke" (15:51-18:52 Uhr) — real waren es 57 Minuten, durchgehend regelkonform unter der geltenden 60-Minuten-Schwelle. Das hätte fast eine Regeldiskussion auf falscher Grundlage ausgelöst. Ein reiner Uhrzeit-Vergleich ist zonenabhängig und im Nachhinein nicht verlässlich prüfbar; ein Minuten-Delta seit dem letzten Fetch ist es nicht — es bleibt unter jeder Zeitzonen-Interpretation gleich. Quelle: [[project_testtag_analyse_2026-08-24]] Abschnitt 2.3 und 9 (Opus-Review, Punkt "Tweet-Fetch-Zähler").

**ADX-Zeile ergänzt (26.08.2026, Fable-Umsetzung nach Opus-Kadenz-Prüfung):** ADX(14, NAS100) wird seit 24.08.2026 als GEMESSEN-KEIN-GATE-Rohwert in `trades.db` gespeichert (`--adx` in `scripts/gate_check.cjs`, siehe dessen Kopfkommentar) und in [[feedback_session_update]] Schritt 6 einmal pro Session gelesen — tauchte bisher aber in keiner laufenden Voll-Check-Ausgabe auf und wurde im Loop faktisch nie gezeigt. Die Zeile oben schließt diese Lücke, exakt nach demselben "GEMESSEN, KEIN GATE"-Prinzip wie die bestehende Trend-Effizienz-Anzeige — keine Schwelle, kein Veto, reine Kontext-Zahl für [[feedback_chartanalyse]] Punkt 8d.

**Prinzip auf zwei weitere Pflichtzeilen ausgedehnt (ergänzt 26.08.2026, siehe [[project_testtag_analyse_2026-08-25]] Abschnitt 6 + 14 Vorschlag 1):** Die Tweet-Check-Zeile war laut Testtag-Analyse 25.08.2026 "die einzige Pflichtzeile in diesem Projekt, die aus einem Urteil einen Rohwert gemacht hat — und die einzige, die an diesem Tag nicht gebrochen wurde". Dasselbe Prinzip (Rohwert + Vergleichsschwelle in derselben Zeile, nicht nur ein Urteil) gilt seit 26.08.2026 auch für die `SL/ATR-Ratio:`-Zeile (siehe [[feedback_chartanalyse]] Punkt 8c, "Format-Verschärfung") und die `Kerze geschlossen:`-Zeile aus Punkt 7d0 (siehe [[feedback_live_trading]], dort "Format-Verschärfung 26.08.2026") — beide gehören nicht zum Voll-Check-Standardformat oben (sie erscheinen nur an Trigger-/Entry-Momenten), folgen aber demselben Formatprinzip wie die Tweet-Check-Zeile hier.

*(Fälligkeitsmechanik erneut abgelöst — Stand 03.09.2026 gilt die DOPPELBEDINGUNG: Raster-Slot echte Minute % 10 == 0 [Kerzenraster-Fix 28.08.2026 bleibt Bedingung (a)] UND Minuten-Delta aus `x_last_fetch.json` ≥10, gerechnet per `node scripts/x_fetch_stamp.cjs --check` — siehe Abschnitt "Tweet-Fälligkeit rechnerisch" unten und die identische Historie-Behandlung in [[feedback_loop_ablauf_uebersicht]] Szenario 1 Schritt 6. Der folgende Absatz beschrieb die Raster-Bedingung noch als ALLEINIGE Fälligkeitsgrundlage und ist insoweit Historie:)* ~~**Kerzenraster-Fassung der Tweet-Check-Zeile (ergänzt 28.08.2026, Levi-Vorgabe nach dem Opus-Review des Testtags 27.08.2026):** Die Vorlage oben ist die ab jetzt geltende Fassung. Die Fälligkeit hängt nicht mehr am Minuten-Delta seit dem letzten TATSÄCHLICHEN Abruf (dessen Ist-Zeitpunkt driftete und erzeugte am 27.08. fünf versäumte Pflicht-Fetches plus falsch gemessene Rohwerte — der `x_last_fetch.json`-Zeitstempel lag bis zu 7 Minuten neben dem echten Schreibzeitpunkt), sondern am festen 5-Min-Kerzenraster: fällig bei jeder zweiten abgeschlossenen 5-Min-Kerze, operativ echte Minute % 10 == 0. Volle Mechanik inkl. Nachhol-Regel: [[feedback_live_trading]] Punkt 9 "Kerzenraster-Fix". Quelle: [[project_testtag_analyse_2026-08-27]] Abschnitt 2.~~

**8d-Format-Fix + Rohwert-Pflicht (ergänzt 28.08.2026, Opus-Vorschlag 4 aus [[project_testtag_analyse_2026-08-27]], Levi-Freigabe):** (a) Der n/3-Zähler der 8d-Zeile ist Pflichtbestandteil und darf nie durch eine Trend/Chop-Einschätzung ERSETZT werden — die ist als Zusatz hinter dem "|" willkommen, beantwortet aber eine andere Frage (Trend vs. Chop statt Schock-Tag ja/nein). Am 27.08.2026 wechselten 9 Voll-Checks in Folge unbemerkt auf die zählerlose Trend/Chop-Fassung. (b) Jeder Zahlenwert in den Pflichtzeilen (ADX, RSI, MACD-H, EMA50) ist ein tatsächlich ABGELESENER Wert oder ein explizites "nicht gelesen" — nie "~X", "implizit", "unverändert niedrig" oder eine Spanne. Am 27.08. standen 3 ADX- und 3 RSI-Angaben ohne echten Ablesewert im Protokoll, und die wichtigste Boundary-Entscheidung des Tages (20:00-Kerzenschluss vs. EMA50) wurde gegen eine EMA50-*Spanne* statt einen abgelesenen Wert getroffen ([[project_testtag_analyse_2026-08-27]] Abschnitte 5+8).

**8d-Einzelkriterien-Klammer (ergänzt 31.08.2026, Opus-Vorschlag 3 aus [[project_testtag_analyse_2026-08-28]], Levi-Freigabe):** Die 8d-Zeile im Template oben trägt seit 31.08.2026 zusätzlich die eckige Klammer mit der Einzelbewertung aller drei Kriterien (Range/ATR-D-Verhältnis mit Rohwerten, Makro-Häufung, VIX-Intraday-Range) — "nicht gemessen" ist für ein tatsächlich nicht geprüftes Kriterium die zulässige, ehrliche Angabe (v.a. VIX-Range) und zählt nicht als erfüllt. **Why:** Am 28.08.2026 stand der n/3-Zähler in 58/58 Checks formal korrekt da, beruhte aber den ganzen Tag auf nur einem tatsächlich gerechneten Kriterium — der Zähler ohne Aufschlüsselung suggeriert eine Prüfung, die zu zwei Dritteln nie stattfand. Volle Regel + Why: [[feedback_chartanalyse]] 8d.

**Voll-Check-Nummer aus der Uhrzeit ableiten, nie mitzählen (ergänzt 28.08.2026, Opus-Vorschlag 8 aus [[project_testtag_analyse_2026-08-27]], Levi-Freigabe):** Die Nummer in der Kopfzeile wird bei jedem Voll-Check aus der echten Uhrzeit berechnet — **Nr. = (Minuten seit dem ersten Voll-Check der Session) / 5 + 1** — und nie als eigener Zähler fortgeschrieben. Fällt ein 5-Min-Slot aus, entsteht so automatisch eine sichtbare Nummernlücke, die im nächsten Voll-Check explizit benannt wird ("Nr. 27 ausgefallen, Grund: X" — siehe auch die Offenlegungspflicht für Protokollbrüche, [[feedback_live_trading]] Punkt 9). **Why:** Am 27.08.2026 sprang die mitgezählte Nummer von (3.) auf (5.) und meldete am Ende "36." bei real 35 Checks — der einzige echte Ausfall (19:15) blieb dadurch unsichtbar, während ein Phantom-Check mitgezählt wurde ([[project_testtag_analyse_2026-08-27]] Abschnitt 1).

**Verschärfung 31.08.2026 — die Rechnung selbst gehört sichtbar in die Kopfzeile (Opus-Vorschlag 4 aus [[project_testtag_analyse_2026-08-28]], Levi-Freigabe):** Das reine Verbot des Mitzählens hat am 28.08.2026 nicht gereicht: Die Nummer wurde dort nicht "mitgezählt" (+1 pro Slot), sondern verschmolz ab 17:45 mit dem Tick-/Fire-Zähler (+2 bis +5 pro 5-Min-Slot — +5 ist die Zahl der Fires: 4 Quick-Ticks + 1 Voll-Check) und erreichte um 19:20 die Nr. 98 statt der formelkorrekten 52. Ein Verbot allein verhindert das nicht — die Nummer muss aus einer Quelle kommen, die nicht mitzählen KANN. Deshalb wird die Formel bei JEDEM Voll-Check neu aus der Uhrzeit gerechnet und **ausgeschrieben in der Kopfzeile mitgeführt**, nicht nur ihr Ergebnis (Template oben): `Voll-Check 18:35 (Nr. 43 = (210 Min seit 15:05)/5+1)`. Wer die Rechnung hinschreibt, kann nicht danebenliegen, und der Prüfer sieht die Herleitung. Positiv-Beleg für die Formel selbst: Nach der 12-Min-Lücke 17:27-17:38 am 28.08. stand der nächste Check formelkorrekt bei Nr. 32 mit sichtbar übersprungenen Nummern 30/31 — erst danach begann die Drift über den Fire-Zähler, den genau diese Verschärfung abschneidet.

**Sekundengenaue Kopfzeile (ergänzt 03.09.2026, Punkt 4 aus [[project_fable_auftragsliste_2026-09-03]]):** Die Uhrzeit in der Kopfzeile (Template oben) ist die sekundengenaue, per bare `date` abgelesene echte Systemzeit (`Voll-Check 16:00:21 …`), nicht nur die Minute und nie ein fortgeschriebener Wert — gilt genauso für die Quick-Tick-Zeile. Volle Zeitanker-Pflicht (date-Abruf bei JEDEM Fire, Nummern/Minutenlogik nur aus Realzeit, Lücken sichtbar): [[feedback_live_trading]] Punkt 9a, "Zeitanker-Pflicht". **Why:** Tag 1 der Testtage 01./02.09. lief mit 19-20 Min Protokoll-Drift (Terminalbedingung "21:45" real um 22:04); an Tag 2 hielt die sekundengenaue Realzeit-Ankerung die Drift unter 1 Minute — per Screenshot-mtimes verifiziert. Bisher war das nur eine Tagesfestlegung des 02.09., jetzt Standard.

**Format-Zeile trägt Nummer + Vollständigkeitsausweis (ergänzt 03.09.2026, Punkt 14 — schließt die im 31.08.-Gegencheck als Punkt 4 benannte, nie geschlossene Lücke):** Die Format-Zeile lautet verbindlich `Format: Fließtext ✓ | Voll-Check #<N> vollständig (MTF ✓ | …)` mit der Voll-Check-Nummer aus der Kopfzeilen-Formel und dem ✓-Ausweis der Pflichtbausteine (Template oben). Eine Format-Zeile OHNE Nummer gilt als fehlende Pflichtzeile (Voll-Check nicht durchgeführt). **Why:** Am 01.09.2026 fehlte die Nummer ab #31 in 54 aufeinanderfolgenden Format-Zeilen — prompt folgte die #45/#44-Nummerndrift, exakt wie schon am 28.08.; am 02.09. waren 84/84 Format-Zeilen nummeriert und die Zählung hielt.

**Sperr-/Halbierungsfenster in der Format-Zeile (ergänzt 03.09.2026, Punkt 6):** Zwischen 15:00 und 15:30 trägt jede Format-Zeile `| Order-Sperre 15:00–15:30 aktiv`, zwischen 15:30 und 16:00 trägt sie `| Halbierungsfenster 15:30–16:00 aktiv` (Pflicht-Halbierung der Position, [[feedback_trading_zeitfenster]]). Der erste Voll-Check ab 16:00 markiert den Ablauf explizit im Fließtext ("Halbierungsfenster abgelaufen — ab jetzt volle Größe zulässig"). **Why:** Die Order-Sperre wurde am 02.09.2026 in jeder Format-Zeile mitgeführt und sauber als abgelaufen markiert — das Halbierungsfenster dagegen NUR einmal in der Vorbereitung und danach nie wieder; der einzige 2/2-Moment fiel um 16:00:21 nur knapp daneben. Fünf Minuten früher hätte kein Mechanismus an die Pflicht-Halbierung erinnert — beim Echtgeld-Start kostet genau das unmittelbar Geld. Dieselbe Mechanik, die bei der Order-Sperre nachweislich funktioniert, gilt jetzt auch hier. *(Ergänzt 14.09.2026: In einem EU/US-Sommerzeit-Divergenz-Fenster nach [[feedback_zeitzone]] — erkennbar am DST-Suffix der T0-Kopfzeile, [[feedback_session_update]] Schritt 0 — tragen die Marker die verschobenen Zeiten UND den Grund, z.B. `| Order-Sperre 14:00–14:30 aktiv (DST-Divergenz)` bzw. `| Halbierungsfenster 14:30–15:00 aktiv (DST-Divergenz)`, und der Ablauf-Hinweis kommt entsprechend im ersten Voll-Check ab 15:00. Offene Code-Inkonsistenz, hier bewusst nicht gefixt, Levi-Entscheidung: `vollcheck.cjs` leitet beide Marker derzeit aus fester DE-Wanduhrzeit ab (Z. 402-404: Stunde 15/Minute <30 bzw. ≥30) und kennt das Fenster nicht — im Divergenz-Fenster würde der Skript-Marker eine Stunde zu spät erscheinen.)*

**Terminalbedingung in der Format-Zeile (ergänzt 03.09.2026, Opus-Vollaudit K-2 — schließt die Lücke aus Punkt 16):** Der Voll-Check, in dessen 5-Min-Slot der feste Terminal-Zeitpunkt des Solo-Mandats fällt (z.B. die 21:45-Regel der Testtage), trägt in der Format-Zeile den eckigen Klammer-Baustein `[| Terminalbedingung 21:45 geprüft ✓]` (Template oben) — zusätzlich zur Fließtext-Pflichtzeile aus [[feedback_live_trading]] Punkt 15d, die beides ausdrücklich verlangt ("im Fließtext UND in der Format-Zeile"). Mechanik identisch zu Order-Sperre und Halbierungsfenster: nur in dem Slot, in dem die Bedingung einschlägig ist. **Why:** Punkt 15d verlangte die Format-Zeilen-Komponente bereits, aber das kanonische Template hier hatte keinen Baustein dafür — exakt die Fehlerklasse, an der die Terminalbedingung am 02.09.2026 real ausfiel (Regel stand nur im Fließtext einer anderen Datei).

**Tweet-Fälligkeit rechnerisch, nicht per Etikett (ergänzt 03.09.2026, Punkt 17):** "fällig" darf in der Tweet-Check-Zeile nur stehen, wenn BEIDE Bedingungen erfüllt sind: (a) Raster-Slot erreicht (echte Minute % 10 == 0, Kerzenraster-Fix 28.08.) UND (b) das Minuten-Delta aus `x_last_fetch.json` ist tatsächlich ≥10 — das Delta liefert `node scripts/x_fetch_stamp.cjs --check` fertig gerechnet (inkl. "fällig JA/NEIN"). Zeigt der Check <10 Min, ist der Slot NICHT fällig, egal was das Raster-Etikett suggeriert. Nach jedem tatsächlichen Fetch wird der Wasserstand ausschließlich per `node scripts/x_fetch_stamp.cjs --set <UTC-Zeitstempel des jüngsten gesehenen Tweets>` geschrieben (Guard gegen Zukunfts-/Ortszeit-Stempel, siehe [[feedback_session_update]] Schritt 2). *(F-B 14.09.2026, Präzisierung: Das Delta für (b) ist der Abstand zum letzten tatsächlichen ABRUF — Feld `last_poll`, bei jedem `--set` bzw. `--polled` [Abruf ohne neuen Tweet] gestempelt —, nicht das Alter des jüngsten Tweets `last_fetch`; gemessen ab dem Beginn des 10-Min-Slots des letzten Abrufs, damit ein verspäteter Raster-Fetch am nächsten Slot nicht an der Rundung kippt. Verpasst-Erkennung/Nachholen ebenfalls gegen `last_poll`. Herleitung: [[feedback_live_trading]] Punkt 9 "Nachtrag F-B".)* **Why:** Am 02.09.2026 degenerierte das behauptete 10-Min-Raster ab VC#27 auf einen faktischen 5-Min-Takt (61 Abrufe auf 84 Voll-Checks statt ~42) — und Über-Polling ist die im Protokoll nicht erwogene, plausible Ursache der drei leeren `@DeItaone`-Antworten. Bei einer leeren Antwort trotz fälligem Fetch gehört diese Hypothese (Über-Polling?) explizit in die Zeile.

**MTF-Frische-Pflichtzeile (ergänzt 03.09.2026, Fable-Finalentscheidung #22 nach Opus-Gegencheck — AUSWEISEN, KEIN Gate):** Die Template-Zeile `MTF-Frische:` weist bei jedem Voll-Check das Alter der `count:2`-Referenzkerze je Ebene aus. **Bezugspunkt verbindlich: Alter = jetzt − SCHLUSSZEIT der Referenzkerze** (= Open-Timestamp + Kerzenlänge; der Open-Timestamp wird seit Punkt 15 ohnehin mitgeführt, [[feedback_loop_ablauf_uebersicht]] Szenario 1 Schritt 2) — nie ab Kerzen-Open, das war die Mehrdeutigkeit, an der die zuerst erwogene Toleranztabelle (5min ≤7 / 15min ≤17 / 1H ≤62 Min) scheiterte. **Schwellen: 2 Kerzenlängen + 2 Min Toleranz → 5min ≤ 12 | 15min ≤ 32 | 1H ≤ 122 Minuten.** Diese Grenzen sind im Normalbetrieb konstruktiv nicht erreichbar (`count:2`-Semantik: Normalfall-Alter < 1 Kerzenlänge ab Schluss, Rollover-Verzögerung addiert höchstens bis knapp unter die zweite) — genau deshalb taugen sie: Eine Überschreitung heißt nicht "Rollover-Pech", sondern **echter Feed-Defekt** (hängender Datenstrom, falsches Symbol, eingefrorene Session) → Protokollhinweis "Referenz veraltet — Feed prüfen", KEIN Gate (Abhilfe gegen legitimen Rollover gibt es nicht: erneutes Lesen liefert dieselbe bestätigte Kerze, ein hartes Gate wäre nur ein Zufallsgenerator gegen Trigger-Momente). **Zusätzlich Pflichtbestandteil: die SPREIZUNG** (max. Altersdifferenz der drei NAS100-Ebenen + QQQ, mit Nennung der ältesten Ebene) — der eigentliche Wert der Zeile: Der VC#13-Fall vom 02.09. (1H/15min sekundenaktuell, 5min-Referenz 5 Min älter) wird von keiner Einzel-Ebenen-Schwelle erfasst, wohl aber von der Spreizungszahl. Fehlt die Zeile, gilt der Voll-Check als nicht vollständig (Format-Zeile trägt `MTF-Frische ✓`).

**8d-Zeile erweitert (03.09.2026, Finalentscheidung #3+#11):** Die 8d-Template-Zeile oben trägt seit 03.09.2026 (a) hinter Makro-Häufung den Event-Ausweis (qualifizierende Events = Blackout-Termin UND dokumentierte Konsens-Abweichung, mit Ist/Konsens — K2 latcht bis Handelsschluss), (b) beim VIX den Richtungsfilter-Ausweis (aktueller VIX vs. Vortagesschluss — nur bei VIX ≥ VT-Schluss kann K3 zählen) und (c) die K3-Schatten-Klammer (GEMESSEN, KEIN GATE: VIX ≥ 20 ODER Δ ggü. VT-Schluss ≥ +10 %). Volle Regel + Kopplungsbegründung (8d-Ratschen-Fix): [[feedback_chartanalyse]] 8d.

**Chasing-Status-Pflichtzeile + Retest-Zeitbox-Baustein (ergänzt 04.09.2026, P6 + P1 der finalen Opus+Fable-Prioritätenliste nach dem Testtag 03.09., siehe [[project_testtag_analyse_2026-09-03]]):** (a) `Chasing-Status (13):` ist in JEDEM Voll-Check Pflicht — der Kerzenzähler kommt aus der Zählstände-Zeile, der k/2-Zähler ist die bisher nie sichtbar geführte 2-Voll-Check-Schwelle aus [[feedback_live_trading]] 13.1; ab k=2 ohne Punkt-11-Kippsignal lautet die Konsequenz zwingend "50 %-Einstieg AKTIV vorschlagen" (Regel: [[feedback_live_trading]] 13.1 "verankert"). Der Wert wandert im Trigger-Moment als `--chasing yes|no` in `gate_check.cjs`. Format-Zeile trägt `Chasing ✓`. (b) Der eckige `Retest-Zeitbox:`-Block erscheint NUR in den zwei Voll-Checks nach einem `gate_check.cjs`-FAIL/UNKNOWN — seit 09.09.2026 (TODO 9, Levi-Bestätigung) auch nach PASS mit Q-Score ROT (dieselbe Mechanik wie Order-Sperre/Halbierungsfenster: nur wenn einschlägig), mit der Bedingung wörtlich aus der `RETEST-ZEITBOX (P1)`-Skriptzeile; die Format-Zeile trägt dann `| Retest-Zeitbox VC+k/2`. VC+2 ohne erfüllte Bedingung endet zwingend mit "VERFALLEN" (Regel: [[feedback_chartanalyse]] 8b "Retest-Zeitbox nach Gate-FAIL/UNKNOWN sowie nach Gate-PASS mit Q-Score ROT", inkl. Absatz "Erweiterung 09.09.2026" und "G5-Korrektur 09.09.2026"). Bei `--position offen` gilt die Pflichtprüfung selbst NICHT (kein `--retest-bedingung`-Zwang, Status `n.a.` informativ) — die Zeitbox ist bei offener Position adressatenlos, ein Retest-Neu-Aufruf ist erst nach Positionsschluss sinnvoll; der erzwungene VERFALLEN-Baustein läuft davon unabhängig weiter [G5-Fix, Option (d)]. **Why:** Am 03.09.2026 wurde 13.1 in 84 Voll-Checks nie zitiert (Interlock mit einer 1H-Eigenauslegung) und auf den VC#23-FAIL folgten 35 Voll-Checks "kein erneuter Aufruf" ohne Verfall — beides "stille" Prüfungen ohne sichtbaren Beleg, dieselbe Fehlerklasse wie Tweet-Check/Format am 27.07. (c) Die Fibonacci-Klausel in der `Chartmuster/Fibonacci:`-Zeile ist seit 04.09.2026 (P5) enger: "ausgelassen" nur ohne Impuls ≥2×ATR mit Korrektur, sonst Extension-Werte + Register-Eintrag ausweisen.

**Register-Check-Pflichtzeile — gerechnet statt behauptet (ergänzt 07.09.2026, Anhang A der Opus-Auftragsliste [[project_fable_auftragsliste_2026-09-07]], bindende Auflage aus der Abbruch-Empfehlung "EINGESCHRÄNKT" der Testtag-Analyse 04.09., Fable-Umsetzung):** Der Register-Baustein der Format-Zeile ist kein Häkchen mehr, sondern trägt das Ergebnis von `node scripts/register_check.cjs` (Exit-Code 0 = OK ≤60 Min / 1 = WARNUNG 60-90 Min → Register in DIESEM Voll-Check per `node scripts/register_touch.cjs --note "VC#<N> …"` erneuern / 2 = HART >90 Min, Zukunftsstempel oder Datei defekt → `gate_check.cjs` würde JETZT mit Exit 1 abbrechen, Register vor dem nächsten Trigger neu schreiben). Die erste Skript-Ausgabezeile steht wörtlich in der neuen `Register-Check:`-Pflichtzeile (Template oben); "weiter gültig"/"unverändert gültig" ist als Formulierung NICHT mehr zulässig — nur das Skript-Urteil. `register_touch.cjs` ist seit 07.09.2026 der einzige vorgesehene Schreibweg für `updated` (echte UTC, lehnt Zukunftsstempel ab, loggt jeden Touch nach `register_touch_log.jsonl` — daraus zählt `protokoll_bilanz.cjs` die Register-Updates des Tages); die Schwellen 60/90 lesen alle drei Skripte aus `scripts/register_constants.cjs`. **Why:** Am 04.09.2026 trugen 17 aufeinanderfolgende Voll-Checks (VC#29-#45) "level_register.json weiter gültig" bei 95-175 Min Registeralter — `gate_check.cjs` kannte beide Schwellen korrekt, prüfte sie aber nur beim Gate-Aufruf; zwischen zwei Triggern prüfte niemand, die Pflichtzeile wurde aus dem Gedächtnis befüllt. Dasselbe Muster wie die Tweet-Check-Zeile (`x_fetch_stamp.cjs --check`): ein Rohwert mit Schwelle statt eines Urteils.

**1H-Schatten-Pflichtzeile (ergänzt 07.09.2026, Punkt 10 der Opus-Auftragsliste — MESSEN statt drehen, KEINE Regeländerung):** Bei jedem Voll-Check wird der Abstand Kurs↔1H-EMA50 absolut und in ATR(5min) sowie das Ereignis "3/4-Blockade" (5min + 15min NAS100 + QQQ richtungsgleich, einzig der 1H-Override dagegen — 04.09.: 1 Blockade, ~100 Min, 222 Pkt) ausgewiesen und als JSONL-Zeile angehängt: `echo '{"ts":"<UTC-ISO>","vc":<N>,"kurs":<x>,"ema50_1h":<y>,"abstand_pkt":<d>,"abstand_atr":<a>,"blockade_3v4":<true|false>,"richtung":"long|short|none"}' >> scripts/oneh_shadow_log.jsonl`. Die Tagesbilanz zählt die 3/4-Blockaden (Dauer, Punkte). **Zähldefinition verschärft (E7, Opus-Gegencheck 07.09.2026):** Eine Blockade zählt nur, wenn sie ≥15 Min andauert UND der Kurs in der blockierten Richtung ≥1,5×ATR(5min) zurücklegt; zu JEDER gezählten Blockade wird beim Tagesabschluss das kontrafaktische Ergebnis nachgetragen (hätte der blockierte Trade TP1 oder SL erreicht?) — beidseitige Auswertung, sonst misst man nur die Kosten der Regel, nie ihren Nutzen. **Entscheidungsschwelle vorab (Opus-Empfehlung, von Levi bestätigt 07.09.2026):** Erst nach ≥5 weiteren Testtagen ODER ≥10 Blockaden wird über die P8-Kandidaten entschieden (i Überschuss-Klausel bei X×ATR + Einigkeit der drei anderen Ebenen / ii räumlich: 1H-EMA50 blockiert nur, wenn sie zwischen Entry und TP1 liegt / iii Strukturanker statt EMA) — vorher keine Änderung am 1H-Override ([[feedback_live_trading]] 7b1, Definition vom 04.09.). Kein Gate, kein Sizing, reine Messung.

**SL-Anker-Vorprüfung — Kollision P4/8c2 vor den Trigger-Moment verlegt (ergänzt 07.09.2026, E3 des Opus-Gegenchecks [[project_gegencheck_fable_umsetzung_2026-09-07]], Fable-Umsetzung Runde 2, **von Levi bestätigt 07.09.2026 — bindend als 7b1 Schritt 0**, siehe [[feedback_live_trading]] Punkt 7b1):** In jedem Voll-Check mit beobachtetem Setup wird der aktuelle Struktur-Anker (7b1 Schritt 3a: tiefstes Low/höchstes High der abgeschlossenen 5min-Kerzen seit dem Impuls, inkl. Docht) per `gate_check.cjs --sl-vorpruefung` gegen die im Register geführten Wick-Zonen (`Wick-Zone (8c2)`, Pflege per `register_touch.cjs --wick-zone`) und den 8c-Floor geprüft; die URTEIL-Zeile steht wörtlich im Template-Block oben. Rechtsfolge und Why: [[feedback_live_trading]] Punkt 2b Item (6f). Kein Gate, keine Schwellenänderung — dieselbe Rechnung, die `gate_check.cjs` live macht, nur früher.

**Erzeugungsweg: `vollcheck.cjs`/`position_tick.cjs` statt Handkomposition (ergänzt 09.09.2026, Fable-Umsetzung TODO 2+5 aus [[project_testtag_analyse_2026-09-08]]; zwei Opus-Gegencheck-Runden [[project_gegencheck_fable_umsetzung_2026-09-09]] und [[project_gegencheck_fable_umsetzung_2026-09-09_runde2]]):** Der komplette Pflichtzeilen-Block oben ist seit 09.09.2026 NICHT mehr von Hand aus dieser und weiteren Regelwerksdateien zu komponieren, sondern per `node scripts/vollcheck.cjs ...` (Voll-Check-Block) und `node scripts/position_tick.cjs ...` (Positions-Kasten inkl. Volumen-Check, Reversal n/4, 7c/12-Stall) zu erzeugen — beide nach demselben A3-Muster wie `gate_check.cjs` (kein Parameter mit stillem Default, fehlender Wert nur mit `--grund-<feld>`). **Why:** Am 08.09.2026 fielen 13 Pflichtbestandteile in 46 von 46 Voll-Checks aus, weil sie von Hand aus vier Dateien zusammengesetzt werden mussten. Ein von Hand komponierter Voll-Check, der wörtlich wie das Template hier aussieht, gilt ab sofort NICHT mehr als ausreichend belegt — die Skript-Ausgabe ist der Nachweis.

**Stand der Skript-Härtung (Stand 09.09.2026 Abend, nach Opus-Gegencheck Runde 4, [[project_gegencheck_fable_umsetzung_2026-09-09_runde4]]):** B1-B11 und C1-C6 sind vollständig behoben und von Opus mehrfach live gegengeprüft (u.a. alle 53+29 A3-Felder einzeln getestet, kompletter Trockenlauf inkl. Register-Refresh-Fall end-to-end durchgeführt — Voll-Check + Positions-Tick liefen sauber durch). D1-D5 sind ebenfalls behoben, bis auf eine Teillücke: D1(a) (SL-Vorprüfung UNTAUGLICH als Normalfall vs. echte 2/2-Abweichung, `protokoll_bilanz.cjs`) war zunächst nur zur Hälfte gefixt — ein Regex ohne Zeilenanfang-Anker zählte die `Stale-Check:`-Rückfallzeile ("Dual-Gate 2/2 + 1H-Override dagegen") fälschlich als echten 2/2-Marker mit. Am 09.09.2026 direkt vom Hauptagenten korrigiert (Anker `^\**Dual-Gate` ergänzt, Fix von Opus vorab verifiziert). **Live-Loop-Pfad (vollcheck.cjs/position_tick.cjs/Register-Refresh/Trigger-Kandidaten-Log) ist damit end-to-end freigegeben.** Verbleibend, ausdrücklich KEIN Blocker: E2 (1H-Schatten zeigt `E7 zählt: ✗` statt "nicht prüfbar" bei fehlendem `--atr`, betrifft nur das Shadow-Log), E3 (`--jetzt` wird nicht gegen die echte Systemzeit plausibilisiert — Härtungsvorschlag für später, verlangt ohnehin einen bewusst falschen, gegen die Zeitanker-Pflicht 9a verstoßenden Anker), zwei dokumentierte Rand-Effekte von D1 (ein 2/2-Zustand, der zwischen zwei Voll-Check-Blöcken entsteht, oder zwei UNTAUGLICH-Momente im selben Block, werden bewusst unterschätzt statt überschätzt — fail-safe in Richtung "keine Abweichung gemeldet", nicht in Richtung falscher Freigabe).

**Kommandotemplates (von Opus im Gegencheck Runde 2 geprüft und live lauffähig verifiziert — Maximalfall mit allen situativen Blöcken, Exit 0, "vollständig"):**

*Template A — `vollcheck.cjs` (bei JEDEM Voll-Check, Minute % 5 == 0):*

Basisblock, alle Felder Pflicht, kein Default (`--state` seit 09.09.2026 Abend Pflicht — TODO 2, siehe Absatz "Stand 09.09.2026 Abend" unten; `--trend-effizienz` ist seit dem Opus-Gegencheck 11.09.2026 OPTIONAL (Chop-Verdacht 8d nur Ausweis, unkalibriert — [[feedback_chartanalyse]] 8d) und steht deshalb im situativen Block unten; ab dem 2. Voll-Check des Tages kommen `--erster-vollcheck`, `--kerzen-nas100`, `--kerzen-qqq`, `--zyklus-8a5`, `--zyklus-8a5-level`, `--k-ohne-signal`, `--impuls-pkt` aus dem Zustandsspeicher und werden NICHT mehr übergeben):
```
node scripts/vollcheck.cjs --state scripts/vollcheck_state.json --jetzt <UTC-ISO mit Z, echte Systemzeit sekundengenau> --erster-vollcheck <UTC-ISO mit Z des ersten Voll-Checks des Tages> --einordnung "<ein Satz zur Lage>" --richtung <long|short|none> --kurs <X> --atr <ATR(14,5min)> --close-1h <X> --ema50-1h <X> --rsi-1h <X> --macdh-1h <X> --struktur-1h "<HH-HL|LH-LL|Range>" --close-15m <X> --ema50-15m <X> --rsi-15m <X> --macdh-15m <X> --close-5m <X> --ema50-5m <X> --rsi-5m <X> --macdh-5m <X> --qqq-gate <offen|zu> --qqq-kurs <X> --qqq-close-15m <X> --qqq-ema50-15m <X> --qqq-vwap-session <X> --qqq-rvol <X> --qqq-session-hoch <X> --qqq-session-tief <X> --qqq-kerze-bestaetigt <ja|nein> --ref-1h <Unix-s, Open der zuletzt GESCHLOSSENEN 1H-Kerze> --ref-15m <Unix-s> --ref-5m <Unix-s> --ref-qqq <Unix-s> --tagesrange <Pkt> --atr-d <X> --range-stand <HH:MM> --makro-events <N> --makro-events-text "<Events|keine>" --vix-range-pct <X> --vix-akt <X> --vix-vt-schluss <X> --regime <trend|chop|...> --adx <X> --zyklus-8a5 <N> --zyklus-8a5-level <Level|none> --kerzen-nas100 <N> --kerzen-qqq <N> --impuls-pkt <X> --k-ohne-signal <N> --tweet-fetch <ja|nein> --position <offen|keine> --screenshot <Pfad|ausgelassen> --terminal-zeit <HH:MM|keine> --fazit "<🟢/🔴/→warten …>"
```

Situative Pflichtblöcke — anhängen, wenn die Bedingung zutrifft (sonst Exit 1):
```
… ≥1 Dual-Gate-Bein bestätigt ODER --richtung long|short:
    --sl-anker <Struktur-Anker nach 7b1 Schritt 3a> --cluster-level <Preis|tief-hoch|register|none> [--tp1-kandidat <X>]
    (11.09.2026, TODO 1/3 + Opus-Gegencheck A-2/A-3: der REGELKONFORME SL-Kandidat = MAX(Anker ∓ 0,5×ATR, sichere
    Cluster-Kante ∓ 0,5×ATR, 8c-Floor) wird per gate_check.cjs --sl-vorpruefung --sl-auto gerechnet und bewertet, der
    Anker ist nie der SL; BEIDE Felder sind Pflicht OHNE --grund-Ausweg (Auflage 3) — --cluster-level ohne Default,
    "none" nur als belegte Session-Antwort mit Begründung im Fließtext, siehe 7b1 Schritt 0)
… optional (Chop-Verdacht 8d, maschinell, unkalibriert — nur Ausweis, Opus-Gegencheck 11.09.2026):
    --trend-effizienz <X>   (ohne Wert: Klammer "Chop-Verdacht nicht bestimmbar", kein LUECKE-Baustein; Rechtsfolge
    Chop nur über --regime chop nach der Netto/Range-Regel)
… --qqq-kerze-bestaetigt nein:
    --spike-b1 <ja|nein> --spike-b2 <ja|nein> --spike-b3 <ja|nein>
… --impuls-pkt ≥ 2×ATR:
    --fib-1272 <X> --fib-1618 <X> --fib-im-register <ja|nein>
… Dual-Gate 2/2 in --richtung, 1H-Override nicht dagegen UND --kerzen-nas100 ≥ 6:
    --stale-n <0-5>
… in den 2 Voll-Checks nach einem Gate-FAIL/UNKNOWN sowie nach Gate-PASS mit Q-Score ROT (Erweiterung 09.09.2026) — NICHT bei --position offen (dort Zeitbox nur informativ "n.a.", Verfall läuft weiter; G5-Fix Option d, 09.09.2026):
    --retest-bedingung <erfuellt|nicht>
… --tweet-fetch ja:
    --tweet-news "<nichts Neues|Hinweis>"
… bekannter Kalender-Release innerhalb ±15 Min (Anlass-Trigger, [[feedback_live_trading]] Punkt 9; F-C 14.09.2026 — das Skript kennt keinen Kalender, der Anlass kommt NUR von hier):
    --tweet-anlass "<Release HH:MM>"   (nie nackt, IMMER mit Uhrzeit; macht den Fetch fällig auch ohne Raster/Delta; den Nachhol-Fall (Slot verpasst) rechnet x_fetch_stamp.cjs selbst — F-A)
      Die Uhrzeit wird GERECHNET, nicht geglaubt (Opus-Gegencheck P2, 14.09.2026): x_fetch_stamp.cjs prüft den Abstand
      Release ↔ Zeitanker gegen ±15 Min. Ohne Uhrzeit = Hard-Exit 1; Release außerhalb des Fensters = KEIN
      Fälligkeitsgrund, ein Fetch darauf bleibt ÜBER-POLLING ✗. Also nur bei einem echten Release im Fenster mitgeben.
… Register-Check meldet Exit 1/2 (Regelfall bei Register vom Vortag):
    `node scripts/register_touch.cjs ...` ausführen, DANACH `vollcheck.cjs` erneut aufrufen **mit frisch per `date` gelesenem
    `--jetzt`** (NICHT den vorherigen `--jetzt`-Wert wiederverwenden — sonst erscheint der gerade geschriebene, echte
    Touch-Zeitstempel relativ zum alten `--jetzt` als "Zukunftsstempel" und `register_check.cjs` meldet erneut HART,
    identische Fehlermeldung, Endlosschleife — D5, Opus-Gegencheck Runde 3, 09.09.2026). Der Touch wird automatisch aus
    `register_touch_log.jsonl` gegen den 5-Min-Slot erkannt. `--register-touch` NICHT mitgeben, außer der Touch ist
    tatsächlich in DIESEM Slot gelaufen — ohne passenden Log-Eintrag KIPPT das Feld die Format-Zeile auf MIT LÜCKEN, bei
    gleichzeitigem Exit 1/2 sogar Hard-Exit 1 (Widerspruchsprüfung). Optional: [--touch-log <Pfad>].
… --position offen UND --qqq-gate offen:
    --rvol-bounce <X> --rvol-haupt <X>
… --terminal-zeit liegt im laufenden 5-Min-Slot ODER ist dessen Ende (nächster Fire = Terminalzeit; TODO 10, 09.09.2026):
    --terminal-geprueft <ja|nein>
… mit --state (Pflicht) UND Chasing-Kriterium erfüllt (kerzen-nas100 ≥ 4):
    --punkt11-signal <ja|nein>   (ja = mindestens EIN Punkt-11-Kriterium erfüllt, NICHT die Dreh-Schwelle 3/4 — 14.09.2026; ja → k-ohne-signal 0, nein → Vorwert + 1; TODO 2)
… neuer Impuls beginnt (Ursprung am Chart bestätigt):  --impuls-ursprung <Preis>   (danach läuft impuls-pkt aus --kurs mit)
… manuell vom State-Wert abweichender Zähler (z.B. Gegenseiten-Schluss, der im Referenz-Delta nicht sichtbar war):
    --<feld> <Wert> --<feld>-reset-grund "<Begründung>"   (feld = kerzen-nas100 | kerzen-qqq | zyklus-8a5 | zyklus-8a5-level | k-ohne-signal | erster-vollcheck)
… --qqq-gate zu:  die acht --qqq-*-Felder, --ref-qqq und --kerzen-qqq entfallen ersatzlos
… --screenshot ausgelassen:  nur zulässig MIT --grund-screenshot "<Begründung>"
… jedes am Chart nicht ablesbare Feld:  NICHT weglassen, sondern --grund-<feldname> "<Begründung>"
    (erscheint als sichtbare Lücke; die Format-Zeile trägt dann "MIT LÜCKEN")
Optional: [--level-register <Pfad>] [--shadow-log <Pfad>] [--json]
```

Die vollständige Skript-Ausgabe wird wörtlich in den Voll-Check übernommen — sie IST der Voll-Check, nicht seine Zusammenfassung. Exit-Code 1 = Voll-Check gilt als nicht durchgeführt.

*Template B — `position_tick.cjs` (bei JEDEM Check-in mit offener Position, Quick-Tick UND Voll-Check):*
```
node scripts/position_tick.cjs --jetzt <UTC-ISO mit Z> --dir <long|short> --entry <X> --sl <X> --tp1 <X> --tp2 <X|none> --kurs <X, aus Chart-Bars, nicht quote_get> --atr <ATR(14,5min)> --eur-pro-punkt <X> --extrem-kurs <Bewegungs-Extrem seit Entry bzw. letztem Teilverkauf> --rsi <X> --rsi-extrem <RSI am lokalen Extrem> --kerzen-ohne-extrem <N> --muster-9d1 <ja|nein> --teilverkauf <ja|nein> --avwap-anker <Unix-SEKUNDEN des Entry-Kerzenbeginns, Rücklesewert in_0|fehlgeschlagen> --qqq-gate <offen|zu> --rvol-bounce <X> --rvol-haupt <X> --qqq-kurs <X> --qqq-ema50-15m <X> --qqq-kerze-bestaetigt <ja|nein> --macdh-5m <X> --macdh-checks-gegen <N> --ema50-5m <X> --folgekerze <ja|nein> --macdh-15m <X> --macdh-15m-vor <X> --checks-7c <N> [--avwap-entry <AVWAP-Wert der Ereignis-Instanz>]
```

Situativ:
```
… statt --eur-pro-punkt:  --stueck <N> --zert <X> --hebel <X>   (Formel (Stück × Zert × Hebel)/Entry aus size.cjs)
… --qqq-kerze-bestaetigt nein:  --spike-b1 <ja|nein> --spike-b2 <ja|nein> --spike-b3 <ja|nein>
… --qqq-gate zu:  --rvol-bounce, --rvol-haupt, --qqq-kurs, --qqq-ema50-15m, --qqq-kerze-bestaetigt entfallen
                  (Reversal-Kriterium 3 entfällt, Schwelle wird 2 von 3)
… nicht ablesbares Feld:  --grund-<feldname> "<Begründung>" statt Weglassen
```

Beide Aufrufe erzeugen ihren Block auf stdout. Für den projektüblichen Datei-Redirect: `> scripts/last_vollcheck.txt 2>&1; echo "Exit-Code: $?"` (PowerShell: `; Write-Output "Exit-Code: $LASTEXITCODE"`) — dafür sind zwei zusätzliche `.gitignore`-Zeilen nötig; ohne Redirect funktioniert die wörtliche Übernahme aus dem Terminal-Output genauso.

**Stand 09.09.2026 Abend — Fable-Umsetzung der 11 Opus-TODOs aus [[project_testtag_analyse_2026-09-09]] (Skript-Seite; Gegencheck durch Opus ausstehend):** (TODO 1) Die beiden Templates oben sind die EINZIGE Quelle — `node scripts/loop_prompt.cjs [--terminal-zeit HH:MM|keine]` liest sie von hier und druckt den vollständigen Loop-Start-Pflichtblock, der wörtlich in den CronCreate-Prompt gehört (Checkliste: [[feedback_live_trading]] Punkt 2b "Loop-Start-Checkliste"). (TODO 2) `--state scripts/vollcheck_state.json` ist Pflicht: Zähler werden aus dem Vorzustand + Referenz-Delta gerechnet (neue Kerzen aus `--ref-5m`/`--ref-qqq`-Differenz, Seitenwechsel aus Close vs. EMA50, k-ohne-signal aus `--punkt11-signal`, Impuls aus `--impuls-ursprung` + `--kurs`), Re-Run desselben VC zählt nicht doppelt, ein State mit anderem DE-Datum wird verworfen; die neue Pflichtzeile `**Zustandsspeicher (--state …):**` zeigt jede Ableitung. (TODO 4) Die Tweet-Check-Zeile entsteht aus dem Unterprozess `x_fetch_stamp.cjs --check --jetzt` (druckt jetzt Delta-, Raster- UND Doppelbedingungs-Zeile) und zitiert dessen Zeilen wörtlich; Widerspruch zu `--tweet-fetch` kippt die Format-Zeile in BEIDE Richtungen (überfällig+nein = ✗, nicht fällig+ja = ✗ Über-Polling — vorher "✓ (Über-Polling)"). (TODO 5) `gate_check.cjs` live schreibt bei PASS + Q-Score ROT einen Eintrag nach `scripts/skipped_setups_fiktiv.jsonl` (ohne DB); Entscheidung + hypothetisches Ergebnis werden beim Tagesabschluss per `node scripts/skipped_fiktiv.cjs --nachtrag <id> --entscheidung ausgelassen|eingestiegen --hoch <H> --tief <T> --terminalkurs <K>` nachgetragen (rechnet MFE/MAE/R selbst; `--list --offen` zeigt Offenes). (TODO 8) Die 1H-Schatten-Zeile nennt Existenz und Eintragszahl von `oneh_shadow_log.jsonl`; `--dry-run` weist die Messung als AUSGESETZT ✗ aus (kippt) — die Datei fehlte nach 2 Testtagen nur, weil das Skript im Loop nie ohne `--dry-run` lief. (TODO 9) siehe Retest-Zeitbox-Zeile oben. (TODO 10) Terminalbedingung erscheint, sobald der Zeitpunkt im laufenden Slot liegt ODER dessen Ende ist (18:55-Voll-Check bei Terminal 19:00), zusätzlich als Fließtext-Pflichtzeile; ein überschrittener Terminal bei laufendem Loop ist ein ✗-Baustein. (TODO 11) Jeder `gate_check.cjs`-Live- und `--sl-vorpruefung`-Lauf hängt sich mit Kommandozeile und kompletter Ausgabe an `scripts/gate_check_log.jsonl` an (auch bei Exit 1), `last_gate_check.txt` bleibt nur Komfort. (TODO 3/6) `protokoll_bilanz.cjs` erkennt das reale Kopfzeilenformat und "weiterhin 2/2", meldet "nicht ermittelbar" statt Falschzahlen und prüft jedes ✓ der Format-Zeile gegen die tatsächlich vorhandene Pflichtzeile im Block.

**Why (Templates):** `gate_check.cjs` musste sein Kommandotemplate fünfmal nachbessern (27.08./31.08. dreifach), jedes Mal mit derselben Diagnose: "wörtlich kopiertes Template war seit dem letzten A3-Fix nicht mehr lauffähig". `vollcheck.cjs` (52 A3-Pflichtfelder + 11 situative Blöcke, läuft 46-84x/Tag) und `position_tick.cjs` (27 Pflichtfelder, ~190x/Tag) haben ein deutlich größeres Fehlerpotential als `gate_check.cjs` je hatte und liefen bis 09.09.2026 Abend ganz ohne kopierbares Template — der bloße Kopfkommentar-Beispielaufruf in `vollcheck.cjs` war selbst nicht vollständig lauffähig (kein `--register-touch` im Normalfall Register-vom-Vortag → Exit 1). Ohne Template hier wäre die Rückfalloption "Handkomposition" — also der 08.09.-Zustand — der wahrscheinliche Ausgang gewesen.

**Zählstände-Pflichtzeile (ergänzt 03.09.2026, Punkt 19):** Die Template-Zeile `Zählstände:` führt die laufenden Messgrößen — 8a5-Reclaim-Zyklenzählung (mit Bezugslevel) und die Kerzenzähler seit letztem Gegenseiten-Schluss für NAS100(5min)/QQQ(15min) — in JEDEM Voll-Check als fortlaufenden Zählstand mit (aus dem Vorcheck übernommen und um das Delta fortgeschrieben, bei Reset mit Grund). Damit stehen `--kerzen-nas100`/`--kerzen-qqq` am Trigger-Moment ablesbar im letzten Voll-Check und können nicht mehr per Messfeld-Ausnahme entfallen. **Why:** An beiden Testtagen entfielen genau diese Felder mit Begründungen wie "im Protokollverlauf nicht lücken- und zweifelsfrei rückverfolgt" — die Tag-2-Analyse nennt das zutreffend ein Rückverfolgbarkeitsproblem des Protokollformats, nicht Faulheit im Einzelfall; diese Zeile behebt das Format, nicht das Symptom.
